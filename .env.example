# ─────────────────────────────────────────────────────────────────────────────
# docker compose 환경변수 템플릿 (루트 .env 로 복사해서 사용)
#
#   다른 PC 사용법:  cp .env.example .env   (그러면 docker compose 가 자동으로 읽음)
#
# 이 파일(.env.example)만 git에 커밋되고, 실제 .env 는 .gitignore 로 제외됨(머신별 오버라이드).
# .env 가 없어도 docker-compose.yml 의 기본값이 적용되므로 그대로 docker compose up 해도 됨.
# ─────────────────────────────────────────────────────────────────────────────

# ── PMS Mart (MongoDB OLAP 사전집계 가속) ──────────────────────────────────────
# true  : ETD/ETA·식별자·거래처·항만·영업 필터 조회를 MongoDB Mart 로 가속(sub-second).
# false : 전 쿼리 OLTP(postgres) — 광범위 조회 시 수 초(느림). mongo 없이도 동작.
# docker-compose.yml 기본값은 true (docker 스택은 mongo 를 항상 동반하므로).
PMS_MART_ENABLED=true

# 라우팅 전략:
#   auto      : Mart 미지원 필터(실적일자=performanceDt·서류일자=documentDt·BMS 운임행/서류
#               필터)만 OLTP 폴백, 나머지는 Mart. (권장)
#   mart-only : 항상 Mart. ⚠ line-level 필터가 무시되어 결과가 부정확해질 수 있어 사용 주의.
PMS_MART_ROUTING=auto

# MongoDB 접속 URI (docker 네트워크 기본값 — 서비스명 mongo)
MONGO_URI=mongodb://mongo:27017/pms_mart

# Mart full rebuild 배치/병렬 (POST /api/pms/mart/rebuild?mode=full 시에만 영향)
PMS_MART_BATCH_SIZE=2000
PMS_MART_PARALLELISM=4

# ── line-grain 가속 (실적일자·서류일자 + BMS 차원 필터) — docker-compose 기본 ON ──────────
# docker 스택은 자가관리: 빈 Mart로 기동하면 백그라운드 자동 full rebuild(그 동안 OLTP 폴백),
# 차있으면 skip. 인덱스는 기동 시 ensureIndex가 자동. → 수동 rebuild·mongosh 작업 불요.
# 끄려면 아래 주석 해제(=OLTP 폴백, 오늘과 동일·무회귀):
# PMS_MART_LINE_ACCEL_ENABLED=false
#
# 증분 동기화 스케줄러 — docker 기본 ON(1분마다 source 변경분 Mart 반영). 끄려면 주석 해제:
# PMS_MART_SCHEDULER_ENABLED=false
#
# 빈 Mart 자동 rebuild 끄기(수동 rebuild만 쓰려면):
# PMS_MART_BOOTSTRAP_AUTO_REBUILD=false
#
# 근사 총건수 $sample 크기(application.yml 기본 2000) / 희소·조기종료 임계 — 보통 불요:
# PMS_MART_APPROX_SAMPLE_SIZE=2000
# PMS_MART_LINE_EARLY_TERM_THRESHOLD=20000

# ── EDMS 첨부 S3 스토리지 (fms-api) ──────────────────────────────────────────
# docker-compose 기본 모드 = s3 (로컬 파일시스템만 쓰려면 local 로 변경).
# s3 모드는 S3 장애 시 로컬 스필오버 자동 폴백 + 회복 시 정리 스케줄러가 자동 승격.
# ⚠ FMS_S3_BUCKET 이 비어 있으면 s3 모드 부팅이 fail-fast 로 실패함 — 실값 필수.
# FMS_STORAGE_MODE=s3
FMS_S3_BUCKET=
FMS_S3_REGION=ap-northeast-2
# key-prefix 는 최초 설정 후 불변(변경 시 기존 객체가 관리 목록에서 빠져 잔존):
# FMS_S3_KEY_PREFIX=
# MinIO/장애 드릴용 endpoint 오버라이드(빈값=실 AWS):
# FMS_S3_ENDPOINT=
# 전용 IAM 사용자 Access Key (s3:Put/Get/DeleteObject + s3:ListBucket 최소권한):
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# 고아 정리+승격 스케줄 cron (기본 매일 03:00, 드릴 시 매분 "0 * * * * *"):
# FMS_STORAGE_CLEANUP_CRON=0 0 3 * * *
