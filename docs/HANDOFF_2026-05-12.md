# 2026-05-12 핸드오프 — Truck B/L 마이그레이션 후 미해결 이슈

## 컨텍스트

- **이번 작업 완료**: Truck B/L Entry 풀 마이그레이션 (Phase A 백엔드 CRUD/EXACT/ChangeBlNo 5 endpoint + Phase B 프론트 schema/toolbar/입력 컴포넌트 표준화 + Phase C 프론트 mutation/Search/ChangeBlNo 결선)
- **커밋**: `b7d00a2` feat(BE) + `b6a8d79` feat(FE) + `ec9e969` docs(ENTRY_MIGRATION_GUIDE)
- **상태**: origin/master 보다 12 commit 앞. push 대기
- **검증**: BE 642/642 PASS, FE lint 0 error / build PASS
- **가이드 등재**: `ENTRY_MIGRATION_GUIDE.md` §10 (Truck B/L 풀 마이그레이션) + §12.2 + §12.3

내일 다른 PC에서 아래 11개 이슈를 이어 처리.

---

## 🔴 즉시 확인 필요 (실제 동작 영향)

### 1. Truck Description Clause ComboBox enum 이름 미스매치

- **위치**: `front-end/src/components/fms/truck-bl/tabs/sections/truck-description-panel.tsx:11`
- **현재 호출**: `useEnumOptions("DescriptionClause")` ← 등록된 이름 아님
- **백엔드 실제 등록 이름**: `DescClause1` / `DescClause2` (`back-end/.../EnumRegistryFactory.java:58-61`, 각 enum 별도 등록)
- **증상**: Description Clause 1/2 ComboBox 옵션 0개로 표시되는 버그
- **처리 방향**:
  - Non B/L Entry가 Description Clause를 어떻게 처리하는지 비교 (단일 통합 enum vs 2개 분리 ComboBox)
  - 패턴 정렬해서 `useEnumOptions("DescClause1")` + `useEnumOptions("DescClause2")` 분리 호출 또는 백엔드에 통합 enum 추가
- **체크 대상 파일**:
  - `front-end/src/components/fms/non-bl/...description...tsx` (Non B/L의 동일 위치)

### 2. TruckBlDetailResponse 자식 응답 누락 (BE 후속)

- **위치**: `back-end/.../adapter/in/web/truckbl/dto/TruckBlDetailResponse.java`
- **누락 필드**:
  - `truckOrders` — Truck Information 그리드 14컬럼
  - `desc` — `house_bl_truck_desc` (marks/description/descClause1/descClause2)
- **증상**: 저장 후 detail 재조회 시 자식 데이터가 form에 미반영. 첫 진입 시 그리드/마크/디스크립션 빈 상태로 표시
- **처리 방향**:
  - BE: `TruckBlDetailResponse` + 매핑 어셈블러 확장. `house_bl_truck_desc` LEFT JOIN 또는 별도 fetch (NonBl `f632ee9` 패턴 — `findById` 시 desc LEFT JOIN 제거 후 별도 QueryDSL 사례 참고)
  - **§6.29 SSOT 필수 적용** — BE 응답 시그니처 변경 시 FE adapter zod 스키마 + Port 반환 타입 **동시 정합**. 어긋나면 mutation onSuccess 미발화로 후속 라우팅·setFocus 분기 깨짐
  - FE: `adapter/out/api/truck-bl.ts`의 detail zod schema + `domain/truck-bl/index.ts`의 `TruckBlDetail` 타입 + `truck-bl-entry.tsx`의 `form.reset` 매핑 동기화
- **가이드 §12.2** 후속 결정 항목 등재됨

---

## 🟡 보고/승인 사후 처리

### 3. NonBlPersistenceAdapterTest 사용자 사후 승인 항목

- **변경**: `back-end/java-spring/src/test/java/com/freightos/fms/adapter/out/persistence/nonbl/NonBlPersistenceAdapterTest.java`의 `emptyCommand()` 파라미터 53→54 (null 1개 추가)
- **원인**: 이번 작업에서 `UpdateHouseBlCommand` record에 `TruckDetailCommand` 추가로 컴파일 회피용
- **CLAUDE.md 위반**: "테스트의 수정 및 삭제는 에이전트 단독 판단이 아닌 사용자의 명시적인 승인을 얻은 후에 진행" 룰에 어긋남 — 사후 보고로 처리됨
- **현재 상태**: 테스트 PASS. 사용자 사후 승인 또는 롤백 의사 표시 필요
- **롤백 시**: `emptyCommand()` 헬퍼를 새로 작성하지 말고 `null` 라인만 제거 (52→53 컴파일 오류 발생할 것이므로 별도 헬퍼 분기 필요)

### 4. pkgUnit 정책 변경 — UX 확인 필요

- **위치**: `front-end/src/components/fms/truck-bl/tabs/sections/truck-cargo-panel.tsx`
- **변경 전**: native `<select>` 7개 옵션 (CTN/PKG/BAG/PLT/BOX/PCS/ROL)
- **변경 후**: `CodeBox kind="code-only"` 자유 텍스트 (Non B/L §10 정책 일치)
- **영향**: 기존 사용자가 드롭다운으로 선택하던 워크플로우 사라짐. 자유 입력 가능하지만 오타 위험·통계 분산 가능
- **결정 옵션**:
  - 유지 (Non B/L 정책 정합 우선)
  - ComboBox + `useEnumOptions("PackageUnit")` 등록 (BE 신규 enum + 등록 필요)
- 회귀 시 사용자 피드백 기반 결정

---

## 🟢 후속 작업 / 기술 부채

### 5. HouseBlFactory.java 335줄 분리 검토

- **위치**: `back-end/.../application/housebl/HouseBlFactory.java`
- **변동**: 305줄 → 335줄 (Truck `applyTruckCreate`/`applyTruckUpdate` 메서드 추가)
- **CLAUDE.md 규칙**: 300줄 초과 분리 검토, 500줄 초과 강제 분리 — 현재 검토 대상
- **권장 분리 형태**: `HouseBlSubFactory` 패턴 (이미 `HouseBlSubFactory.applyDesc` 존재) — Sea/Air/Truck/NonBl 별 `HouseBl<JobDiv>SubFactory`로 분리하고 `HouseBlFactory`는 dispatcher 역할로 축소

### 6. truck-bl-entry.tsx 237줄 분리 검토 가능

- **위치**: `front-end/src/components/fms/truck-bl/truck-bl-entry.tsx`
- **상태**: 300줄 미만이라 강제 아님. 향후 mutation/search/changeBlNo 추가 분기 시 임계 근접 우려
- **분리 후보**: useTruckBlEntry hook으로 form/state 응축 — Non B/L Entry도 동일 패턴 미적용이므로 두 도메인 함께 처리 권장

### 7. ChangeBlNoModal 도메인 공통화 (§12.3)

- **현재 중복**: `front-end/src/components/fms/non-bl/change-bl-no-modal.tsx` + `front-end/src/components/fms/truck-bl/truck-change-bl-no-modal.tsx`
- **원인**: 두 파일 모두 `port`/`queryKey`/`label` 하드코딩 → prop 분기 불가
- **공통화 방향 (2가지 검토)**:
  - A) 도메인 prop 주입: `<ChangeBlNoModal port={...} queryKey={[...]} label="Non B/L" />`
  - B) useMutation 외부 주입: `<ChangeBlNoModal mutation={changeMutation} ... />`
- **결정 시점**: 세 번째 도메인(House B/L Sea/Air, Master B/L 등) 적용 시
- **가이드 §12.3 등재**

### 8. 다른 Entry 풀 마이그레이션 잔여 — 다음 작업 대기열

`ENTRY_MIGRATION_GUIDE.md` §10에 등재된 Non B/L · Truck B/L 외 잔여:

- **Sea House Entry** — 패턴 B(query-param 부분 마이그레이션, §6.26)
- **Air House Entry** — 패턴 B
- **Sea Master Entry** — 패턴 B
- **Air Master Entry** — 패턴 B
- **SwitchBlModal** — `window.confirm` 잔존 (§959 공용 confirm 모달 적용 권장)

각 Entry에 §6.35 도메인 전용 Port+Adapter 패턴(NonBl/Truck 외) 적용 권장. 체크리스트는 ENTRY_MIGRATION_GUIDE §6.35 참고.

**Entry Search EXACT PK 패턴(§6.19)**, **ChangeBlNo 흐름(§930)**, **hot-marker(§6.16)**, **공용 confirm 모달(§959)**, **detail useQuery staleTime:Infinity(§6.36)** 다섯 패턴이 모든 Entry에 미적용 상태 — 마이그레이션 시 일괄 적용.

### 9. List DTO 분리 계획 점검 (memory `project_list_dto_split.md`)

- **계획**: 6 범주 × 12 DTO 목표
- **메모리 시점**: Truck·Non B/L 진행 중(2026-05-05) — 현재 진행 상태 확인 필요
- **잔여**: Sea House / Air House / Sea Master / Air Master List DTO 분리
- **작업 시 확인**: 메모리 정보가 stale 가능 → 실제 코드 grep으로 진행 상태 검증 필수

### 10. 22개 fms 테이블 빈 컬럼·빈 테이블 채움 plan (memory `project_22tables_fill.md`)

- **계획**: `.claude/plans/master-bl-select-noble-map.md`
- **분할**: 5 Phase 확정
- **상태**: plan 예약 — 실행 미시작 (house_bl_license는 별도 제거로 목록에서 제외)
- 별도 plan으로 진행

---

## ⚪ 운영/메타

### 11. 메모리 stale 발견 — 다른 도메인 List 점검 권장

이번 작업에서 다음이 stale로 판명:

- `ENTRY_MIGRATION_GUIDE.md §10` "Truck B/L List·Non B/L List 더블클릭 시 hot-marker SET" 표기 있었으나 **Truck B/L List는 실제 미적용 상태**였음. Phase C에서 `truck-bl-grid.tsx`에 1줄 추가 보정
- 가이드/메모리는 "선언된 사실"이지 "현재 코드 상태"가 아님 — 다음 마이그레이션 작업 전 grep으로 실제 적용 여부 검증 필수

**점검 권장 대상**:
- `front-end/src/components/fms/{sea-house,air-house,sea-master,air-master}-bl/...grid.tsx`의 `onDoubleClick`에 `sessionStorage.setItem` 호출 존재 여부
- 메모리 룰: "Before recommending from memory ... If the user is about to act on your recommendation, verify first."

---

## 권장 처리 순서

1. **이슈 1 + 2 묶어서 한 PR** — Truck Description Clause enum 이름 수정 + TruckBlDetailResponse 자식 응답 확장 (§6.29 정합). BE/FE 동시 작업
2. **이슈 3** — 사용자 승인 (테스트는 이미 PASS 상태라 후순위 가능)
3. **이슈 4** — 사용자 피드백 후 결정
4. **이슈 5 + 7** — 마이그레이션 진행 중 자연 처리 (강제 아님)
5. **이슈 6** — 향후 분량 증가 시 처리
6. **이슈 8** — 별도 plan으로 진행 (가장 큰 작업 단위)
7. **이슈 9 + 10** — 별도 plan으로 진행
8. **이슈 11** — 다음 마이그레이션 시 점검 의무

---

## 참고 위치

- **가이드**: `ENTRY_MIGRATION_GUIDE.md` §10(완료 목록) / §6(함정·SSOT) / §12.2 / §12.3
- **메모리 인덱스**: `C:\Users\a0102\.claude\projects\C--vive-coding-portfolio-legacy-to-next-legacy-to-next\memory\MEMORY.md`
- **이번 작업 plan**: `C:\Users\a0102\.claude\plans\idempotent-beaming-balloon.md`
- **이번 작업 커밋**: `git log --oneline -3` → `ec9e969` / `b6a8d79` / `b7d00a2`
- **Backend enum 등록**: `back-end/java-spring/src/main/java/com/freightos/fms/application/enums/EnumRegistryFactory.java`
- **Non B/L 패턴 SSOT**: `front-end/src/components/fms/non-bl/` 전체, `back-end/.../application/nonbl/*` + `adapter/out/persistence/nonbl/*`

---

## 다음 단계 (한 줄 요약)

내일 PC에서 가장 먼저 1번(Description Clause enum 미스매치) 확인 → 2번(TruckBlDetailResponse 자식 응답 확장)과 묶어서 BE/FE 한 PR로 처리. 나머지 9개는 우선순위 낮음.
