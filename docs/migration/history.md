# History — 이미 완료된 공통 작업

> **언제 read**: "이미 완료된 작업"이 무엇인지 확인이 필요할 때 (디테일 검증 / 새 도메인 패턴 참고). 일반 마이그레이션 사이클에서는 read 불필요.

본 파일은 ENTRY_MIGRATION_GUIDE.md §10의 정보성 로그를 그대로 이관한 것. 차기 마이그레이션 commit 후 본 history에 1줄 요약 추가 의무.

---

## 10. 이미 완료된 공통 작업 (다른 Entry에서 재호출 가능)

다음은 이미 적용되어 있어 다른 Entry 마이그레이션 시 재구현 불필요:

- DateBox 카탈로그 컴포넌트 (`shared/inputs/date-box.tsx`) — **`variant="cell"` 지원 추가됨 (2026-05)**
- BoxBaseProps 표준 (`shared/inputs/_types.ts`)
- **표준 입력 컴포넌트 default `autoComplete="off"`** (TextBox/NumberBox/CodeBox/DateBox + grid-cell-inputs.tsx의 TextCell/NumericCell/DateInputBase) — cell variant 사용 시 자동 적용. Native `grid__cell-input` 직접 사용 시 §6.12 참고
- **그리드 셀 SSOT 결정**: TextBox/NumberBox/DateBox/ComboBox/TimeBox `variant="cell"`. TextCell/NumericCell/DateCell은 `@deprecated` (§6.12 참고)
- 그리드 셀 편집 모드 단일 outline 처리 (`grids.css`)
- 그리드 외부 클릭 selection 자동 해제 (`use-grid-cell-selection.ts`)
- 그리드 외부 클릭 행 highlight 해제 (`onClearRow` prop)
- PlainGridList 컬럼 리사이즈 (session-local)
- selection overlay td DOM 실측 보정
- 컬럼 리사이즈 후 overlay 자동 갱신 (ResizeObserver)
- CodeBox lcn / party-cn 라벨 너비 통일
- **ComboBox `variant="label"` 지원** — LcnLabel 라벨 셀렉터 위임 (2026-05-08, 00a9a16)
- **BoxVariant 타입 확장** — "panel" / "cell" / "label" 3종 (2026-05-08, 3c78d06)
- **ComboBox RHF Controller 패턴 SSOT** — register spread 금지, Controller 필수 (2026-05-08, 66a217c)
- **Non B/L Entry hot-marker 라우팅 표준화** — F5 클리어 / 저장 직후 재조회 / New URL 리셋 / Search hot-marker 재사용 / unmount clearDraft (2026-05-09, c918324·0526e83·4572a33)
- **Entry form Enter 키 implicit submission 차단** — `form onKeyDown` 가드(TEXTAREA 제외) + Save 버튼 `type="button"·onClick` 이중 방어 (2026-05-09, faad158)
- **CreateNonBlRequest validation 어노테이션 UI required 외 제거** — UI required 아닌 필드는 어노테이션 없이 null 통과. VO 검증 유지. ⚠️ 0aafb4d는 UI required 9개도 함께 제거한 over-correction. (2026-05-09, 0aafb4d)
- **CreateNonBlRequest UI required 9개 어노테이션 복원** — §6.25 정책 교정. `hblNo`/`workDivision`/`polCode`/`podCode`/`etd`/`eta`/`actualCustomerCode`/`operatorCode`/`teamCode`에 `@NotBlank`+형식 어노테이션 재부착. (2026-05-09)
- **List 더블클릭 → Entry 단건 조회 모드 진입 (§6.26)** — Non B/L List·Truck B/L List 더블클릭 시 hot-marker SET 후 path-param push. Truck B/L 백엔드 `GET /api/truck-bl/{id}` 신규 추가(`TruckBlUseCase`, `TruckBlService`, `TruckBlFactory`, `TruckBlDetailResult`, `TruckBlDetailResponse`). Truck Entry edit-mode 결선(id prop, hydrateAllowed, useQuery, form.reset, F5-redirect, clearDraft). (2026-05-09)
- **Non B/L Entry body 콤보박스 4종 + Ref.No 저장 누락 보정 (2026-05-09)** — `volumeDivisor` Non B/L 한정 도입(`HouseBlNonBl`), `salesClass`·`pkgUnit`·`containerType` projection 복원, `mblNo`/`masterRefNo` `assignMasterReference` 호출 보강, `dimensionDivisor` FE Controller 전환. §12.1에 항공 공통화 검토 메모 추가.
- **Non B/L 단건 조회 desc LEFT JOIN 제거 + remark 필드 이전 (2026-05-10, f632ee9)** — `HouseBlJpaEntity.desc` `@OneToOne mappedBy` 의 EAGER 한계로 `findById` 시 `house_bl_desc` LEFT JOIN이 자동 발사되던 현상. NON_BL 화면이 desc(marks/description/descClause1/descClause2) 미사용 사실 확인 후, NON_BL 단건 조회를 `NonBlSearchPort.findNonBlById` QueryDSL projection 으로 분리하고 `house_bl_desc` JOIN 차단. NON_BL 의 `remark` 필드는 `house_bl_desc.remark` 대신 `house_bl_non_bl.remark` 로 이전 (DDL `V_add_house_bl_non_bl_remark.sql` 신설). 응답·요청 DTO(`NonBlDetailResponse`/`CreateNonBlRequest`/`UpdateNonBlRequest`)에서 desc 객체 제거하고 본체에 `remark` 평탄화. SEA/AIR/TRUCK 무영향(`HouseBlSubFactory.applyDesc` 에 `instanceof HouseBlNonBl` 가드 추가). FE 도메인 타입·`buildNonBlRequest`·Entry `methods.reset`·`adapter/out/api/non-bl.ts` 매핑 동기화.
- **Non B/L Entry remark 바인딩 후속 fix (2026-05-10, 16753a4·93f7e68)** — f632ee9 후 zod schema에 `remark` 누락 + Entry 화면 `methods.reset` 매핑 미정합으로 저장 후 조회 시 remark 미표시. zod schema에 `remark` 추가 + desc 잔재 제거 + 화면 설정 값 정합 보정.
- **Non B/L Entry 자식 collection UPDATE 쿼리 누락 fix (2026-05-09, 67aa1d4·2eefab1·0e55a28)** — Dimension·Container·Desc 자식 row의 PUT 페이로드에 DB id가 빠져 백엔드 매퍼가 신규로 인식 → UPDATE 미발사. FE 자식 행에 `id` 포함 + 백엔드 어댑터 매퍼 보정 + 어댑터 테스트를 `merge*` 메서드 호출 검증으로 갱신. (§6.28 SSOT 함정으로 등재)
- **Entry 화면 house_bl_id URL 노출 제거 (2026-05-09, 1482b65)** — Entry path-param에 내부 PK(`house_bl_id`)가 노출되던 동작을 `hbl_no` 기반 식별로 통일. List 더블클릭 → Entry 진입(§6.26) 패턴과 정합.
- **Entry 라우팅·사이드바·탭 stable 동작 fix (2026-05-09, d084934·14250d7·b393795)** — (1) 사이드바 메뉴 이동 시 Entry store 유지·탭 닫기 시 store clear, (2) List 더블클릭 Entry 이동 시 List 탭이 소멸하던 버그, (3) 동일 케이스에서 사이드바 섹션이 닫히던 버그 모두 수정. §6.26 보강.
- **§6.25 SSOT DDL 정합 후속 (2026-05-09, dd3f3b2·3510adc)** — Non B/L Entry 저장 시 백엔드 검증 SSOT 처리 + `house_bl_container.container_no` JPA `nullable=false` 제거 (DDL 마이그레이션과 정합).
- **ScreenGuard 공통 컴포넌트 도입 + Entry 4개 적용 (2026-05-10)** — 비동기 처리 중 화면 잠금 + bar wave 인디케이터 표준화. `@/components/shared/screen-guard` 신규, Tailwind v4 `globals.css` `@theme` 블록에 `waveBar` keyframes/animation 추가, `/preview` 카탈로그에 ScreenGuard 섹션 등록. Non B/L · Truck B/L · House B/L · Master B/L Entry 4개에 `<ScreenGuard visible={isLoading} message={loadingMessage} />` 패턴으로 일괄 적용. **List 화면 적용 금지** (스켈레톤 로딩 사용). 막대 색 `#38BDF8`, 텍스트 색 `#F1F5F9` 가독성 fix. (§6.27 SSOT)
- **House B/L 계열 Entry hbl_no Update 차단 (2026-05-10)** — 모든 House B/L 계열(House BL Sea/Air/Truck, Non B/L)의 toolbar `hbl_no`는 **Insert·조회 입력 전용**이며 update 시 변경 불가. 차단은 **백엔드 SSOT**: `HouseBl.update()` 의 hblNo PATCH 라인 제거 + `HouseBlUpdateFields`·`UpdateHouseBlCommand`·`UpdateHouseBlRequest`·`UpdateNonBlRequest`·`HouseBlFactory`·`HouseBlAssembler`·`NonBlAssembler` 의 hblNo 필드/매핑 제거. FE toolbar `hbl_no`(`hbl`/`nonBlNo`) TextBox는 readOnly 금지(조회 키 입력 + 신규 저장 용도). FE submit은 update 전용 빌더(`buildHouseBlUpdateRequest`/`buildNonBlUpdateRequest`)에서 destructure로 hblNo를 제외해 payload에서 빠진 채 전송 — `isEdit` 인자 패턴 금지(분기 책임은 entry mutation이 가진다). 회귀 보호로 `HouseBlTest.update_doesNotChangeHblNo` 추가.
- **Non B/L Entry Change BL No 모달 + 전용 엔드포인트 (2026-05-10)** — 일반 Update 경로는 hbl_no 변경 차단(위 항목)을 유지한 채, 사용자 명시 변경 액션을 위한 **별도 격리 엔드포인트** `PUT /api/non-bl/{id}/hbl-no` body `{hblNo}` → `ApiResponse<Void>` 신설. 도메인은 `HouseBl.changeHblNo(BlNumber)` 추상 부모에 두어 재사용 가능, 본 작업은 `NonBlController`만 노출(다른 모드는 application 레벨에서 미노출). `NonBlService.changeNonBlHblNo`가 `findNonBlDomainById`로 NON_BL jobDiv 보장 후 `HouseBlService.changeHblNo`에 위임. validation BE SSOT — `ChangeNonBlHblNoRequest` `@NotBlank`+`@Size(max=50)`. **PK(`house_bl_id`) 단일 행 select & update**로 동일 hbl_no가 여러 행에 존재해도 다른 행 무영향(통합 테스트 `NonBlChangeHblNoIntegrationTest`로 회귀 보호). FE 신규 컴포넌트 `change-bl-no-modal.tsx`(현재 hbl_no readOnly + 신 hbl_no 입력 + Update/Close), `nonBlPort.changeHblNo` 어댑터, Entry toolbar 버튼 가드 — 미조회 상태 클릭 시 `toast.info("먼저 Non B/L을 조회해주세요.")` 후 차단(disabled 속성 미사용, 조회된 경우만 모달 오픈). 모달 입력은 zodResolver/required 금지(BE SSOT). 성공 시 `["non-bl","detail",id]` + `["non-bl","list"]` invalidate.
- **HouseBl desc cascade 분리 — Repository 명시 save/delete 전환 (2026-05-10, 701b5e2)** — §587(NON_BL 한정)을 SEA/AIR까지 확장. `HouseBlJpaEntity`의 `@OneToOne(cascade=ALL, orphanRemoval=true) desc` 매핑과 `mergeDesc`/`replaceDesc` 메서드 제거. `HouseBlDescRepository` 신설(`findByHouseBl_HouseBlId`/`deleteByHouseBl_HouseBlId`). `HouseBlPersistenceAdapter`는 SEA/AIR 단건 조회 시 desc 1회 별도 SELECT, NON_BL/TRUCK은 desc 미사용이므로 조회 생략. 저장 경로는 `saveOrDeleteDesc(domainDesc, parentJpa)` private 메서드로 통일 — 도메인 desc=null이면 row 삭제, 있으면 기존 row UPDATE 또는 신규 INSERT. 삭제 경로는 FK ON DELETE CASCADE 미설정으로 `deleteByHouseBl_HouseBlId` 명시 호출. `HouseBlJpaToDomainMapper.toSeaDomain/toAirDomain` 시그니처에 `HouseBlDescJpaEntity descJpa` 인자 추가.
- **Change B/L No 흐름 QueryDSL 부분 UPDATE 단축 + form stale fix (2026-05-10, 86d4406·d7eb0b9)** — §595 후속. (BE) `HouseBlPort.updateHblNoById` 신설, `HouseBlRepositoryImpl`에 QueryDSL `update().set(hblNo).where(houseBlId=:id AND jobDiv=:div).execute()` 첫 도입. `HouseBlService.changeHblNo`·`NonBlService.changeNonBlHblNo` 본문을 도메인 hydrate→full save에서 port 직접 호출로 교체. jobDiv 검증을 UPDATE WHERE 절에 포함하고 affected=0이면 `ResourceNotFoundException`. bulk UPDATE 1차 캐시 stale 방지 위해 `EntityManager.flush()/clear()` 호출. NON_BL Change BL No 기준 SELECT 7회 → 0회, UPDATE 1회. (FE) `ChangeBlNoModal`에 `onChanged?: () => void` 콜백 prop 추가, `NonBlEntry`에서 `onChanged`로 `detailLoadedRef.current=false` 트리거 → invalidate 후 refetch된 detail이 `form.reset`에 반영. list 캐시 자동 invalidate 라인 제거(§6.21 준수).
- **Non B/L Entry update 누락 필드 후속 fix (2026-05-10, a71bb0d·55b53e5)** — §588 후속. (a71bb0d) `updateNonBl` 경로에 `bound`/`workDivision`이 빠져 변경이 사라지던 현상 — `HouseBlUpdateFields` record + `HouseBl.update()`에 bound PATCH 분기 추가, `HouseBlNonBl.updateWorkDivision()` 신설, `HouseBlFactory.toUpdateFields()` bound 매핑 + `applyNonBlUpdate()` workDivision 갱신 호출 보강. (55b53e5) FE Non B/L Entry `form.reset`에 `refNo: detail.originalBlRef` 매핑 추가(저장 후 화면 미반영 수정). `toBeRequest`의 cargo `volumeWtKg` JSON 키를 BE 명세 `volumeWeightKg`로 정합(BE Jackson 역직렬화 누락 → DB null 덮어쓰기 현상 해소).
- **Non B/L Entry Search EXACT PK 조회 endpoint 분리 (2026-05-10)** — Entry Search가 List용 `searchNonBlSummaries`(LIKE+COUNT, JOIN+22 projection)를 재사용하던 흐름을 폐기하고 `POST /api/non-bl/find-by-hbl-no` 신설. UseCase/Port/Repository에 `findNonBlKeysByHblNoExact(String): List<Long>` 추가, JOIN/projection/COUNT 모두 제거하고 `select house_bl_id where job_div='NON_BL' and hbl_no=? order by created_at desc limit 2`만 발사. FE `nonBlPort.findByHblNo` 어댑터 추가, Entry `handleSearch`의 30개 dummy filter 객체 제거. Detail 로드는 기존 useQuery `getById` 흐름이 그대로 담당. SQL 2회 → 1회. (§6.19 갱신)
- **Non B/L Entry Update 응답용 중복 reload 제거 (2026-05-10)** — Non B/L Update 1회당 p6spy SELECT 6회 발생 분석. dirty checking 사전 fetch(parent + ext entity, line 46/189/126)는 필수, containers/dims lazy(line 81/85)는 응답에 필요하므로 유지. 진짜 redundant한 두 곳만 제거: (A) `HouseBlPersistenceAdapter.saveHouseBl` 본문 끝의 `return loadWithExt(savedJpa)` — NON_BL case 한정으로 `return nonBl;` (in-memory 직접 반환)으로 단축, `BaseEntity.assignIdentity`로 parent id + 감사 필드 sync, 신규 private `syncChildIds`/`syncDimIds`로 cascade flush 후 자식 PK를 도메인 자식에 역방향 인덱스 매핑 sync. SEA/AIR/TRUCK case는 기존대로 `return loadWithExt(savedJpa)` 경로 유지(회귀 차단). (B) `NonBlService.updateNonBl`의 `houseBlUseCase.updateHouseBl` 호출 + `findNonBlDomainById` 응답 재조회 둘 다 제거 — `NonBlService`가 `HouseBlPort` + `HouseBlFactory`를 직접 사용해 `findHouseBlById → applyToEntity → saveHouseBl`을 수행하고 반환 도메인을 `(HouseBlNonBl)` 캐스팅해서 `NonBlDetailResult.from(...)` 호출. ARCH1 — `HouseBlUseCase`에 도메인 노출용 메서드 추가는 금지(application↔application 참조는 허용). 결과: **SELECT 6 → 5회 (실측)**. line 141의 `loadWithExt` 자체는 자체 SELECT를 만들지만 그 내부 라인(189/81/85)은 사전 로드 시 이미 1차 캐시·lazy 초기화 완료되어 두 번째 호출에서 SELECT 미발사 → 실제 절감은 NonBlService 응답 재조회(#6, line 157의 QueryDSL fetchOne) 1건. dirty checking 사전 fetch(#5, line 126)는 영속성 컨텍스트 managed 상태 확보 목적이라 변경 데이터 유무와 무관하게 발사되며, 본 작업에서는 의도적으로 유지(추가 제거는 옵션 3 — 사전 로드된 ext JPA를 saveHouseBl 시그니처로 전달해 1차 캐시 히트 유도, 별도 plan). UPDATE 변경 없음(house_bl + house_bl_non_bl + container/dim merge). 테스트 정리 — `HouseBlPersistenceAdapterTest`의 NON_BL save 3개에서 `jpaToDomainMapper.toNonBlDomain` stubbing 라인 제거(`UnnecessaryStubbingException` 해소). `loadWithExt` 메서드 자체는 SEA/AIR/TRUCK 및 `findHouseBlById` 경로에서 그대로 사용.
- **Non B/L Entry 신규 저장 응답 detail 재조회 제거 (2026-05-10, 608d0df·1be98fb·7e5e41d)** — POST `/api/non-bl` 응답 body를 `ApiResponse<NonBlDetailResponse>` → `ApiResponse<{id}>`로 축소, `nonBlUseCase.findNonBlById(id)` 재조회 호출 제거 → BE 응답 흐름 SELECT 3건(house_bl×non_bl join + containers LAZY + dims LAZY) 절감. 화면 detail 갱신은 컴포넌트 `onSuccess`의 `setFocus("nonBl", saved.id)` → useQuery `getById` refetch가 담당. FE adapter `non-bl.ts` `create` zod 파싱 스키마 `NON_BL_DETAIL_SCHEMA` → `apiResponse(z.object({ id: z.number() }))`, `NonBlPort.create` 반환 `Promise<NonBlDetail>` → `Promise<{id: number}>` 동시 정합. `non-bl-entry.tsx`는 `saved.id`만 사용하므로 무수정. web layer 테스트 1건(`createNonBl_happyPath_returns201WithIdAndLocation`) 추가 — Mockito `any(Class)` 매처는 null 미매칭 함정이 있어 `any()` 사용. (§6.29 SSOT 함정으로 등재)
- **HouseBl 삭제 SELECT 최적화 시리즈 (2026-05-10, 2920bf9·07dd2dd·d9f9cd4)** — NON_BL 삭제 SQL 분석 후 단계 최적화. (1) 2920bf9: `deleteHouseBl`에 JobDiv 분기 도입 + ext bulk delete (work_division 무관 4개 ext 모두 SELECT-then-delete 패턴 폐기, NON_BL 삭제 SELECT 12→8회). (2) 07dd2dd: 5개 ext Repository(sea/air/truck/non_bl/desc)의 derived `deleteByXxx`를 `@Modifying @Query` JPQL bulk DELETE로 교체 — derived deleteBy의 SELECT-then-delete 패턴 회피. (3) d9f9cd4: bulk DELETE의 `clearAutomatically=true` 옵션 제거 — 1차 캐시 stale 회복으로 부모 deleteById의 cascade fetch가 캐시 hit (8→7회). (§6.30 SSOT 함정 등재)
- **ER 재구조화 Phase 1 — 단독 자식 ext FK 이전 (2026-05-10·11)** — HouseBl/MasterBl의 1:N 자식이 부모 FK로 매달려 있어 cascade orphanRemoval로 JobDiv 무관 모든 자식 LAZY fetch가 트리거되는 근본 문제 해결의 첫 단계. 단독 사용 자식 5개를 ext PK FK로 이전(테이블명 유지). Step 1.1 schedule_leg→AIR (9bb5436·1a4207f), 1.2 air_charge→AIR (7cb1281), 1.3 truck_order→TRUCK (3335073), 1.4 master_schedule_leg→AIR (3884e83), 1.5 master_air_charge→AIR (14beff9·b9491b8). 각 step 마이그레이션은 컬럼 추가/백필/NOT NULL/FK CASCADE/기존 FK·컬럼 제거 5단계. AIR/TRUCK 분기 호출 순서: ext save → sync* (PK 확보 후, `savedAirJpa`/`savedTruckJpa` 변수 도입). (§6.32 SSOT 등재)
- **ER 재구조화 Phase 2 — desc 분리 (2026-05-10·11, c07f701·b0ebc0f·7b6d0b6)** — desc는 여러 JobDiv 공유 자식이라 테이블 자체를 ext별로 분리. Step 2.1: HouseBl `house_bl_desc` → `sea_desc`+`air_desc`+`truck_desc` 3개. Step 2.2: MasterBl `master_bl_desc` → `sea_desc`+`air_desc` 2개. NON_BL은 desc 미사용. 도메인 `HouseBlDesc`/`MasterBlDesc` 그대로, adapter/mapper만 ext별 분기. `deleteHouseBl`에서 desc 명시 호출은 ext bulk DELETE에 위임. 7b6d0b6에서 5개 desc JPA에 `@OnDelete(CASCADE)` 추가는 H2 `ddl-auto` 재생성 회피 목적이었으나, 후속 옵션 D 작업에서 DDL_RULES §5 위반으로 모두 제거됨(§6.31 갱신 SSOT 참고). (§6.32 SSOT 등재)
- **ER 재구조화 Phase 3 — container 분리 (2026-05-10·11, 7d61e4b·ec6d06f)** — container는 SEA + NON_BL 공유 자식. `house_bl_container` → `house_bl_sea_container`+`house_bl_nonbl_container` 2개로 분리. SEA는 sync 패턴(`syncContainers`), NON_BL은 merge 패턴(`mergeContainers`, id 매칭 update + 신규 insert) 보존. SeaHouseRepositoryImpl QueryDSL Q타입/FK 경로 변경(`QHouseBlContainerJpaEntity` → `QHouseBlSeaContainerJpaEntity`, `houseBlId` → `houseBlSeaId`). ec6d06f: 신규 container entity 2개의 `@NoArgsConstructor` public 접근성 fix(PROTECTED → public, 다른 패키지 mapper의 `new` 호출 컴파일 오류 회피). MasterBl은 container 미사용으로 영향 없음.
- **ER 재구조화 Phase 4 — dim 분리 (2026-05-11, 2b7f35e·a3e3a6c·afb0066)** — dim 처리: HouseBl(공유)는 분리, MasterBl(단독)은 FK만 이전. Step 4.1: HouseBl `house_bl_dim` → `air_dim`+`truck_dim`+`nonbl_dim` 3개로 분리, AIR/TRUCK은 sync 패턴, NON_BL은 merge 패턴. Step 4.2: MasterBl `master_bl_dim` 테이블 유지 + FK만 `master_bl_id` → `master_bl_air_id` 이전(AIR 단독, Phase 1 패턴). 모든 신규 ext 컬렉션 매핑에 `cascade=ALL`+`orphanRemoval=true` 적용(ORM 레벨, save 경로 의존). DB `ON DELETE CASCADE`/JPA `@OnDelete`는 후속 옵션 D 작업에서 모두 제거됨(§6.31 갱신 SSOT). 도메인 `HouseBlDim`/`MasterBlDim` 그대로.
- **ER 재구조화 최종 상태 (2026-05-11, Phase 1~4 완료)** — 모든 1:N/1:1 자식이 의미상 속한 ext의 PK FK로 매달림. 부모 `house_bl`/`master_bl`의 cascade 컬렉션 매핑 0개 → JobDiv 무관 LAZY fetch 0건. **자식 정리는 후속 옵션 D 작업에서 어댑터 명시 bulk DELETE로 전환**(§6.31 갱신 SSOT). **기술 부채 보고**: `HouseBlPersistenceAdapter.java` 347줄, `MasterBlMapper.java` 338줄 — 300줄 임계 초과(500 미만으로 강제 분리 X). 후속으로 `HouseBlPersistenceAdapter`의 `saveOrDelete<Sea/Air/Truck>Desc` 추출 + `MasterBlMapper` → `MasterBlDocMapper` 분리 검토 권장.

- **DDL_RULES §5 정합화 + ID-only 삭제 흐름(옵션 D) (2026-05-11)** — ER 재구조화 Phase 2~4가 DB `ON DELETE CASCADE`+JPA `@OnDelete`에 의존하던 흐름을 §5 준수로 전환. 16개 ext FK의 `ON DELETE CASCADE` 제거(`schema/V1__fms_initial_schema.sql` 통합본으로 14개 마이그레이션 파일 정리). 10개 JPA(desc 5 + ext 5)의 `@OnDelete` 어노테이션·import 제거. `HouseBlPort`/`MasterBlPort`의 `delete<X>Bl(<X>Bl)` 시그니처 폐기 → `deleteByIdAndJobDiv(Long id, JobDiv jobDiv)`+`findJobDivById(Long): Optional<JobDiv>` 신설. Adapter는 `switch(jobDiv)` 분기마다 자식 subquery bulk DELETE → ext bulk DELETE → 부모 bulk DELETE 순서로 명시 호출. Service 계층은 `findJobDivById` projection 1회 후 port 직접 호출(기존 `findEntityById`/`loadWithExt` 호출 제거). `NonBlService.deleteNonBlById`는 자기 jobDiv 알므로 `houseBlPort.deleteByIdAndJobDiv(id, JobDiv.NON_BL)` 직접 호출(HouseBlUseCase 우회). 자식 Repository 13곳에 `deleteByParentHouseBlId`/`deleteByParentMasterBlId` subquery bulk DELETE 메서드 추가(11개 신규 Repository + 5 desc Repository 메서드 추가). 테스트는 cascade 검증을 어댑터 명시 호출 검증으로 일괄 교체(`HouseBlPersistenceAdapterTest`/`MasterBlPersistenceAdapterTest`/`HouseBlServiceTest`/`MasterBlServiceTest`/`MasterBlMappingIntegrationTest`+신규 `NonBlServiceTest`). 성능: NON_BL 삭제 기준 SELECT 4 + DELETE 4 → SELECT 0 + DELETE 4(`HouseBlService` 경유 시 jobDiv projection 1회만 추가). (§6.31 SSOT 갱신)

- **pkgUnit/weightUnit 필드 분리 + Non-BL Cargo UI 재배치 시리즈 (2026-05-11, f6c3657·4423db9·9f88b76)** — `CargoSummary.packageUnit` 단일 필드가 포장 단위(자유텍스트, 'CTN' 등)와 중량 단위(`WeightUnit` enum, KGS/LBS)의 의미를 혼재시키던 문제를 3-step 으로 해소. (1) f6c3657: `WeightUnit.fromCodeOrDefault(String)` 신설(unknown/null/blank → KGS 기본값 반환, `IllegalArgumentException` 제거) + `CargoSummary.packageUnit` → `weightUnit` 리네임(타입-필드명 정합), HouseBl/MasterBl 매퍼·팩토리 5곳의 `fromCode` 호출을 `fromCodeOrDefault` 로 교체 — Non B/L List 더블클릭으로 Entry 진입 시 invalid pkg_unit('CTN' 등)이 던지던 500 에러 즉시 해소. (2) 4423db9: 의미적 분리 완성 — `CargoSummary`/`HouseBl`/`MasterBl` 도메인에 `pkgUnit(String, 자유텍스트)` + `weightUnit(WeightUnit enum)` 두 필드로 분리, `HouseBlSea`/`MasterBlSea`에 있던 `weightUnit`을 **부모 BL로 승격**(Air/Truck/NonBl도 모두 weightUnit 보유 — 모드 무관 공통 필드). DDL: `house_bl`/`master_bl`에 `weight_unit` 컬럼 추가, `house_bl_sea`/`master_bl_sea`에서 제거. BE 어셈블러·매퍼·DTO(Create/Update Request, Detail Response)·Command·Projection·Factory + FE 도메인·schema·api 어댑터·Entry 컴포넌트 일괄 정합화. (3) 9f88b76: Non-BL Cargo 패널 UI 재배치 — `CodeBox` 에 `kind="code-only"` variant 추가(코드 한 칸 + 검색 아이콘, name 영역 미렌더), `non-bl-schema` 의 `cargoUnit` → `weightUnit` 리네임 + `pkgUnit` 신규 필드, `non-bl-submit`/`non-bl-entry`/`non-bl-defaults` 의 pkgUnit·weightUnit 분리 매핑(기존 pkgUnit=undefined 손실 수정), Cargo 패널에서 Package 옆 ComboBox 제거 → `CodeBox kind="code-only"` (pkgUnit), Gross W/T 옆 ComboBox 신설(weightUnit). `CodeBox` 테스트 케이스 추가.

- **HouseBl Bound 필드 NON_BL Entry 필수화 (2026-05-11)** — `CreateNonBlRequest`/`UpdateNonBlRequest`의 `bound`에 `@NotBlank` 추가(DB는 이미 NOT NULL이나 API 검증 누락 NPE 위험). FE `non-bl-schema` `bound: z.string().min(1, "Bound를 선택하세요.")` + 툴바 `is-required` 클래스/라벨 + ComboBox `rules.required` 연동. 신규 의존성 `@hookform/resolvers ^5.2.2` 추가(react-hook-form v7 호환, zodResolver). `NonBlBoundValidationTest` 4 케이스 신규(bound null/blank × create/update). 기존 `NonBlControllerTest.VALID_CREATE_JSON`에 `"bound": "EXP"` 추가(사용자 명시 승인). §6.25 정책 적용 — UI required 필드에 @NotBlank 유지.

- **Sea Master Entry post-migration UI 정리 (2026-05-15)** — Sea Master Migration(9 Phase) 완료 직후 사용자 직접 검수 기반 UI/배치 정리 phase. 6 commit:
  - `a551176f` PackType ENUM 오바인딩 제거 — `pkgUnit`은 BE String 컬럼인데 FE에서 `useEnumOptions("PackType")` 호출로 `EnumRegistry not found: PackType` WARN 발생. ComboBox → CodeBox `kind="code-only"` 전환. G/W 옆 WeightUnit ComboBox 추가(sea-cargo-panel 패턴 동등). Co-Load Agent UI 제거. 단독 80px wrapper + master-cargo-doc-panel scope class + `widgets.css` `.master-cargo-doc-panel .li__input--tight > div > .lcn { display: block; width: 100% }` override 추가(forms.css `.lcn` grid 110px 첫 컬럼이 80px wrapper 무력화하던 문제 해소).
  - `95b7910d` Sea Master Schedule terms 그룹 제거 — Phase 8 toolbar에 동일 enum 필드 추가로 Schedule 패널의 Load/Service/B-L Type 그룹 중복 → 패널 측 제거(toolbar SSOT).
  - `2fc64aa1` Master Entry 레이아웃 배치 조정 — header 버튼 재배치(New/Search/Save/Delete/Change B/L No, Copy 제거) + tabbar Last saved meta 제거 + Party Clear 제거 + Description of Goods 부지약관 DescClause1/2 두 ComboBox 가로 배치(해상 하우스 패턴) + Schedule 패널 줄 단위 widget 분할(Liner/Vessel·Voyage/ETD·ETA/POR/POL/POD/Final Dest/Issue Date+빈칸 EXP만, cols=1 적용으로 lcn name 풀폭) + Port atoms `kind="code-only"` → `kind="lcn"` + OnboardDate 제거 + Cargo & Document Document 영역 Settle/Operator/Team `CodeBox kind="lcn"` + `+ New HBL` 버튼 제거 + Reset 버튼 제거.
  - `eb01adfc` Sea Master Liner / Operator / Team 필수 처리 — `CreateMasterBlRequest.operatorCode`에 `@NotBlank(groups = SeaMasterGroup.class)` 추가(linerCode/teamCode는 기존 적용). FE 시각 `required` prop 추가(LinerLcnField + Operator/Team CodeBox). BE 회귀 분석 — `SeaMasterGroup` 검증을 실제 구동하는 테스트 없어 회귀 0건.
  - `2a86c919` House B/L List 14컬럼 배치 + Container 패널 신규 + main-sea layout 재배치 — House BL List 그리드: `<colgroup>` 너비 강제(120/160/160/160/80/80/100/100/100/100/120/100/100/100px) + isSea 분기 제거(모드 통일 14컬럼) + schema 미존재 6컬럼 빈 셀(후속 phase). Container 패널 신규: `master-container-grid.tsx`(매개변수 0개 함수, `ComponentType<WidgetProps>` 호환 확인) + ReadOnly + +/- 없음 + 10컬럼 colgroup. main-sea layout: Container `col=2 row=4 colSpan=4 rowSpan=2`, schedule/cargo-doc `rowSpan=4→2`, marks/goods/remark `row` 재조정.
  - **신설 SSOT**: 
    1. `.lcn` 폭 80px wrapper 사용 시 panel scope CSS 필요(forms.css `.lcn`이 `grid-template-columns: 110px 120px minmax(0, 1fr)`로 110px 첫 컬럼 강제) — 외부 wrapper만으로는 무력화됨. 해상 하우스 패턴(`.sea-cargo-panel .li__input--tight > div > .lcn { display: block; width: 100% }`) 그대로 차용.
    2. WidgetDef.component는 매개변수 0개 함수도 호환(ComponentType<WidgetProps>) — ReadOnly grid처럼 props 미사용 시 시그니처 단순화 가능.
    3. FieldItemGrid `cols={1}` 활용으로 단독 줄 widget의 lcn name 풀폭 표시 — 단독 줄 widget마다 cols=1, 2칸 줄은 cols=2(기본) 유지.
    4. `useFieldLayout`/`useWidgetLayout` localStorage(`fms.fieldLayouts.v1` 등) stale로 widget 구조 변경 시 빈 행/잘못된 위치 잔여 — version bump 또는 defaultOrder 변경 감지 자동 invalidate 후속 phase. 우선 DevTools 키 삭제 또는 시크릿 창 안내.

- **HouseBlPersistenceAdapter Strategy 분리 (2026-05-11, a8f04c7·2d5ba76)** — 352줄 어댑터의 SEA/AIR/TRUCK/NON_BL switch 분기 3곳(saveHouseBl 96줄·deleteByIdAndJobDiv 28줄·loadWithExt 27줄)을 4개 `HouseBl<JobDiv>PersistenceStrategy`로 추출 + dispatcher 패턴. Strategy 인터페이스(`adapter/out/persistence/housebl/strategy/`)는 package-private로 adapter 외부 노출 차단. 어댑터는 142줄로 축약(공통 `HouseBlRepository`+`HouseBlDomainToJpaMapper`+4 Strategy만 주입), 기존 16+ Repository는 strategy로 분산. 트랜잭션은 어댑터에 `@Transactional` 유지, Strategy는 미부착. Port 시그니처/호출자(NonBlService, HouseBlService 등) 무변경. 통합 테스트 `NonBlChangeHblNoIntegrationTest`/`NonBlFindByHblNoIntegrationTest` `@Import` 블록에 Strategy 4개 추가 필수(누락 시 `NoSuchBeanDefinitionException`). `HouseBlPersistenceAdapterTest`(644→369줄, dispatcher 검증만)+신규 4개 `HouseBl<JobDiv>PersistenceStrategyTest`(각 ~140줄)로 검증 분리. 동작 동등성: p6spy SQL 횟수/순서 변화 없음. (§6.33 SSOT 등재)

- **NonBl update 1차 위임 시도 + 응답 void화 (Phase 2, 2026-05-11, a6d96cd·4165b8a·db98df4)** — `HouseBlPort.update(Long id, Consumer<HouseBl> mutator)` 추가하여 Service의 fetch+mutate+save 3단계를 1줄 위임으로 축약 시도. Controller 응답 `ApiResponse<NonBlDetailResponse>` → `ApiResponse<Void>`(FE invalidate+refetch라 영향 없음). FE `nonBlPort.update`/api client 반환 `Promise<void>`, zod 응답 검증 제거. `non-bl-entry.tsx`의 create(Promise<{id}>)+update(Promise<void>) 단일 `useMutation` 삼항 패턴이 TS2322 제네릭 불일치 → create/update mutation 분리. **함정**: 1차 캐시 hit이 PK 기반 조회만 작동(§6.34)하므로 adapter의 `update`가 내부적으로 `saveHouseBl`을 호출하면 `existsById`(count SELECT)+`strategy.saveExt`의 `findByHouseBlHouseBlId`(FK 메서드 쿼리) 2건이 그대로 발사 → SELECT 절감 0. Service에 `instanceof HouseBlNonBl` 검증과 `factory.applyToEntity` 호출이 남아 위임도 표면적. **Phase 2 보강 작업으로 폐기**됨.

- **NonBl update 진짜 위임 + SELECT 절감 6→4 (Phase 2 보강, 2026-05-11, 3037a44·b0a92ad·8b70969)** — Phase 2의 Consumer trampoline 안티패턴을 폐기하고 **NonBl 전용 Port + Adapter** 신설. `application/nonbl/port/out/NonBlPersistencePort.update(Long id, UpdateHouseBlCommand command)` + `adapter/out/persistence/nonbl/NonBlUpdatePersistenceAdapter`(명명 충돌 회피 — 같은 패키지의 `NonBlPersistenceAdapter`가 `NonBlSearchPort` 구현 중). Service는 `nonBlPersistencePort.update(id, command);` 1줄 위임 + `houseBlFactory` 의존 제거. Adapter가 jobDiv 검증·factory.applyToEntity·attached JPA 직접 매핑까지 일괄 처리하며 `saveHouseBl` 미호출 — dirty checking이 트랜잭션 종료 시 UPDATE 자동 발사. `existsById`(count SELECT) + `findByHouseBlHouseBlId`(FK 재조회) 2건 제거. `HouseBlPort.update(Consumer)` 및 어댑터 구현은 미사용으로 제거(`HouseBlPersistenceAdapterTest`의 update 2 케이스도 제거 — 메인 메서드 제거에 따른 자연 정리, 사용자 승인됨). 신규 `NonBlPersistenceAdapterTest` 6 케이스(happy path·containers·empty collections·notFound·ext notFound·wrong jobDiv). 함정: `emptyCommand()` 헬퍼의 `UpdateHouseBlCommand` 인수 수 정확히 일치 필요(50/53 → 컴파일 실패), Mockito strict mode에서 미사용 stub `UnnecessaryStubbingException` 제거. mergeContainers/mergeDims LAZY 재트리거 없음(`toNonBlDomain`이 이미 1차 캐시 적재). **adapter→application/factory 의존**은 실용적 헥사고널 완화로 사용자 합의. 헥사고널 정통 회피하려면 service에서 command→fields 변환 후 fields만 port에 전달하는 패턴(분량 더 큼, 별도 작업). (§6.33·§6.34·§6.35 SSOT 등재)

  **다른 도메인(HouseBl Sea/Air/Truck · MasterBl · SwitchBl) 적용 권장 — 별도 작업으로 진행. 체크리스트는 §6.35.**

- **Non B/L Entry 공용 confirm 모달 적용 + Copy 제거 + 조회 전 Save 가드 (2026-05-11, 16dbc0b)** — `e55af73` 에서 도입된 `@/components/confirm` 의 **product 첫 적용 사례**. (1) `use-non-bl-entry-mutations.ts` 의 `handleSubmit`/`handleDelete` 를 async 화하고 `await confirm({...})` Promise API 호출 — Save `variant: "default"` + 기본 메시지, Delete `variant: "destructive" + confirmText: "삭제"` + "삭제된 데이터는 복구할 수 없습니다." description. 기존 `window.confirm` 폐기. 반환 타입 시그니처 `handleSubmit`/`handleDelete` → `Promise<void>` 로 정합. (2) `non-bl-entry-header.tsx` 의 onClick 미정의 Copy 버튼(스텁) + lucide-react `Copy` import 제거 — props 인터페이스 변경 없음(`onCopy` 원래 미존재). Copy 자리는 추후 다른 기능으로 대체 예정. (3) `non-bl-entry.tsx` 의 `onSave` prop 을 inline 가드 함수로 교체 — `!isEdit` 시 `toast.info("먼저 Non B/L을 조회해주세요.")` 후 early return, isEdit 일 때만 `methods.handleSubmit(handleSubmit)()` 호출(`handleChangeBlNo` 와 동일 가드 패턴, toast import 기존 재사용). `useNonBlEntryMutations` 의 `createMutation` 코드는 dead code 화되지만 추후 신규 생성 워크플로우 재활성화 가능성으로 **의도적 유지**. ScreenGuard(§6.27) 와 충돌 없음 — confirm 모달은 mutate 외부에서 해소 후 모달 닫힘 → 기존 `isSavePending`/`deleteMutation.isPending` 로딩 표시 흐름 그대로 동작. 다른 Entry(Sea/Air HBL, Master BL, Truck BL, SwitchBlModal) 의 `window.confirm` 잔존 — 동일 패턴 후속 적용 권장.

- **Truck B/L Entry 패널 디테일 정합화 시리즈 (2026-05-12)** — Phase A 풀 마이그레이션 후 패널/그리드 디테일 정합. (1) **Remark 패널 분리 + LineNumberTextarea 통일** (b942e34·d4fb855) — Description 패널에서 Remark 필드 분리해 신규 `truck-remark-panel.tsx`(2x2, Performance 아래 col:4 row:4)로 추출, 처음 TextArea 평문 사용했다가 Description/Marks/Party Address와 동일하게 `Controller + LineNumberTextarea`로 통일. (2) **Party 패널 Non B/L 패턴 채택** (6a1b49c·d1844ed·3df603a·69d8eea) — CodeBox `kind="party-cn"`이 자체 `.party-block`+`.party-block__head`+라벨까지 렌더하므로 외부 wrapper 제거(라벨 중복 해소), Clear 버튼 제거, 잔존 액션 버튼(To Order/Same as Cne.)을 CodeBox 옆 같은 행에 인접 배치(flex gap:4) + Address LineNumberTextarea와의 사이 marginTop:4 추가, `.truck-party-panel .field-widget-item + .field-widget-item { padding-top: 4px }` 스코프 CSS 신설. (3) **Performance 패널 6필드 모두 LCN 통일** (22a2886) — `<div className="li">+TextBox` → `<CodeBox kind="lcn" label="..." required? ...>` 단독 호출 6개, `customerPic`은 schema에 Name 없어 nameProps 미지정(빈 Name input 유지, 추후 master 데이터 join 정책). (4) **Cargo Package/G/W 우측 박스 80px 고정** (91bcd90·0710cb7, §6.42·§6.43 등재) — `.li__input--tight > *` 강제 flex 덮어쓰기 + `.truck-cargo-panel` 스코프 CSS로 `.lcn` grid 해제. (5) **Truck Information 그리드 카탈로그 정합** (86e6493, §6.38) — +/- 버튼 `btn--icon btn--success`/`btn--icon btn--danger`, GridList `onClearRow={() => setSelectedKey(null)}` 추가. (6) **truckType/containerType ComboBox(cell) + BE enum 바인딩** (39469e5·d0dcf1b, §6.45·§6.46) — `useEnumOptions("TruckType")`/`useEnumOptions("ContainerType")` 호출, ContainerType label=description이라 `rawOptions.map(o => ({ value: o.value, label: o.value }))` 재매핑, register spread는 ComboBox value 미주입 → Controller 패턴으로 교체.

- **GridList SSOT 보강 시리즈 (2026-05-12, 5f48185·12e3047·aefe8e9·746a304·056b648)** — Truck B/L Entry 디테일 정합 중 발견한 GridList 공통 버그 4건 일괄 해소. (1) **outside-click 같은 패널 액션 버튼까지 outside 처리** (5f48185, §6.40) — `handleOutsideClick` 가드 추가, `e.target`과 tableEl 둘 다 `closest(".panel")`로 동일 panel인지 확인 후 outside 제외. (2) **useVirtualizer default getItemKey index 기반으로 인한 행 삭제 후 stale input value 표시** (12e3047, §6.41) — `getItemKey` 옵션 추가(rowKey > row.id > index 폴백), row identity 변화 시 React가 element를 unmount/remount → register가 mount 시 RHF latest value를 defaultValue로 적용. PlainGridList + ManagedGridList 둘 다 수정 → 모든 호출처 자동 정상화. (3) **포커싱 셀 행 우선 삭제 패턴 8 그리드 + 카탈로그 일관 적용** (aefe8e9·746a304, §6.39) — Truck Information / Non B/L Dimension·Container Info + House B/L Container·Dimension·Item HS + Master House B/L + 카탈로그 `grid-preview-panel`에 `focusedRowKeyRef` + `captureFocusedRow()` + - 버튼 `onMouseDown` 패턴 일관 적용. handleRemove 우선순위: 포커싱된 td의 row → selectedKey state → 마지막 행. Master House B/L Grid는 커스텀 `<table>` 구조라 `<tr data-row-key={field.rhfKey}>` 마크업도 함께 추가. (4) **Non B/L Dimension/Container Info 그리드 +/- 버튼 카탈로그 정합** (056b648, §6.38) — `btn btn--sm btn--ghost` → `btn btn--sm btn--icon btn--success`/`btn--icon btn--danger`.

- **CodeBox kind 명칭 컨벤션 등재 (2026-05-12, §6.44)** — 사용자가 코드박스 형태를 호명할 때 사용하는 표준 명칭(LCN / code-only / party-cn)을 CodeBox `kind` prop과 1:1 매칭. 메모리 `feedback_codebox_terms.md` 신설.

- **Truck B/L Entry Dimension 그리드 + Volume Divisor 추가 (2026-05-12)** — Truck B/L Entry 풀 마이그레이션 직후 후속. Performance 패널 아래에 Non B/L `nonbl-dimension-panel.tsx`와 동일 구성의 `truck-dimension-panel.tsx`(7컬럼: #/Length/Width/Height/Qty/CBM/Volume Wt., Volume Divisor ComboBox + Plus/Minus toolbar, GridList variant=cell) 2x2 신규(`col:4 row:4`). 기존 Remark는 row:4→6 이동(크기 유지), 다른 패널은 col 영역이 달라 무영향. **(BE)** 인프라(`house_bl_truck_dim` 테이블·`HouseBlTruckDimJpaEntity`·`HouseBlCargoMapper.toTruckDimDomain/toTruckDimJpa`·`HouseBlTruckJpaEntity.@OneToMany dims`+`syncDims`·`HouseBlTruckPersistenceStrategy.saveExt`·`TruckBlUpdatePersistenceAdapter.syncDims`)는 ER 재구조화 Phase 4(§ER §1246)에서 이미 완성되어 있었고, `TruckBlAssembler.toCreate/toUpdateCommand`에서 `null, // dims`/`null, // volumeDivisor` 하드코딩 라인만 풀어주면 데이터 흐름이 살아남. `CreateTruckBlRequest`/`UpdateTruckBlRequest`에 `String volumeDivisor` + `List<DimRequest> dims` + nested `DimRequest` record(Update 버전은 `Long id` 포함, NonBl §6.28 패턴) 추가. `TruckBlDetailResult`/`TruckBlDetailResponse`에 `String volumeDivisor` + `List<DimView> dims` + nested `DimView` record 추가, `TruckBlDetailResponse.from()`/`TruckBlFactory.toDetailResult()` 매핑 연결. `HouseBlTruck` 도메인에 `private VolumeDivisor volumeDivisor` 필드 + `assignVolumeDivisor` 메서드 추가(**dims 필드는 추가하지 않음** — `HouseBl` 부모의 dims 컬렉션 재사용, Non B/L과 동일 SSOT). `HouseBlTruckSubFactory.applyTruckVolumeDivisor(HouseBl, String)` 별도 메서드 신설(기존 `applyTruckCreate`/`applyTruckUpdate` 시그니처 무변경 — ripple 최소). `HouseBlFactory.toEntity`/`applyToEntity`에서 호출 1줄 추가. JPA Entity `HouseBlTruckJpaEntity`에 `@Column(name="volume_divisor", length=10) @Enumerated(STRING)` 필드 + setter, 양방향 매퍼(`HouseBlDomainToJpaMapper.applyTruckFields`/`HouseBlJpaToDomainMapper.copyTruckFields`)에 각 1줄. **DDL**: `house_bl_truck.volume_divisor VARCHAR(10)` 컬럼 추가, `schema/manual_migration/add_truck_volume_divisor_2026-05-12.sql` 운영 마이그레이션 신설(`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`). NULLABLE이며 기존 row backfill 정책은 FE가 `?? "CM6000"` 디폴트 표시 후 다음 Save 시 자연 채움(명시적 UPDATE 미실행). **(FE)** `front-end/src/domain/truck-bl/index.ts`에 `TruckBlDimView`/`TruckBlDimRequest` 인터페이스 + `TruckBlDetail`/`CreateTruckBlRequest`에 `volumeDivisor?`/`dims?` 추가(`UpdateTruckBlRequest`는 `Omit`/`Pick` 자동 반영). `adapter/out/api/truck-bl.ts`에 `TRUCK_BL_DIM_SCHEMA`(zod) + `TRUCK_BL_DETAIL_SCHEMA`에 `volumeDivisor`/`dims` 필드. `truck-bl-schema.ts`에 `TRUCK_DIM_SCHEMA`(string 6필드 + id) + `TRUCK_BL_SCHEMA`에 `dimensionDivisor`/`dimensions` + `EMPTY_TRUCK_DIM_ROW` export. `truck-bl-defaults.ts`에 `dimensionDivisor: "CM6000"`/`dimensions: []`. `use-truck-bl-entry.ts` `form.reset` 매핑(BE→Form: `lengthCm→length string` 등, `detail.volumeDivisor ?? "CM6000"`). `truck-bl-submit.ts`에 `buildTruckDimCreateRows`/`buildTruckDimUpdateRows` 헬퍼 + Create/Update 빌더에 `volumeDivisor`+`dims` 포함. `truck-panels.tsx` 배럴에 export 1줄. **레이아웃**: `main-truck.tsx` TRUCK_REGISTRY에 `dimension-truck { col:4, row:4, colSpan:2, rowSpan:2 }` 신규 + `remark-truck` row:4→6. BE 전체 빌드/테스트 PASS, FE lint 0 error/build PASS. 회귀 검증: `TruckBlFactoryTest`/`TruckBlControllerTest` 등 record 시그니처 변경 컴파일 영향 0건(record 생성자 직접 호출 없음). **§12.1 결정 갱신**: VolumeDivisor가 이제 Non B/L + Truck B/L 2개 도메인에 도입됨 — 항공 마이그레이션 시 `HouseBl` 부모로의 공통 승격 검토 가치 강화. **§12.2 결정 갱신**: `TruckBlDetailResponse`에 `dims`/`volumeDivisor`는 이제 포함됨, 자식 컬렉션 `truckOrders` + `desc`(marks/description/clauses)는 여전히 미포함(별개 후속 작업으로 남음).

- **Sea House FE↔BE 결합 정합화 후속 라운드 (2026-05-13, e0d13c4·04a8127)** — 본체 commit 54e61fb 후 사용자 검증 라운드에서 누적 발견된 결함 16건 일괄 정리. **(e0d13c4 — 11 fix)** (BE) ① `EnumRegistryFactory` ContainerType 등록 value `getCode()`→`name()`, label `description`→`getCode()` (다른 enum TruckType/SalesClass과 SSOT 일관, **Sea House + Truck B/L 두 화면 동시 영향**). `ContainerType.fromCode`를 dual 매칭(valueOf 우선 + getCode fallback)으로 확장 — 외부 입력 호환 유지. ② `HouseBlDetailResult`/`HouseBlDetailResponse`에 `incoterms`/`salesClass` 노출 (Create/Update Request·Assembler엔 있었으나 detail 응답 미노출). (FE) ③ FE form key 정합 7건 — party-panel `ROLE_FIELDS *Addr → *Address`, schedule-panel 본체 `vesselName`/`voyNo` → `seaDetail.vesselName`/`seaDetail.voyageNo`, marks-panel `marksAndNumbers` → `desc.marks`, description-panel `descriptionOfGoods` → `desc.description`, sea-remark-panel `desc.remark` → 본체 `remark`, submit 빌더에 `remark` 라인 추가, schema/defaults 중복 키 7종 제거. ④ `HOUSE_BL_DETAIL_SCHEMA` 9 필드 `.optional()` → `.nullable().optional().transform(v ?? undefined)` 패턴 통일 — zod `.optional()`은 undefined만 허용하고 null 거부, BE NonBl 본체 필드(linerCode/linerName/vesselName/voyageNo/finalDestCode/finalDestName/finalEta/volumeWeightKg/rton)가 SEA 응답에서 null로 도착하던 zod parse 실패 해소. ⑤ HouseBlDetail.blType strict enum(`'OBL'|'SWB'|'SURRENDER'`) → `string|null` 완화 + zod `z.string().nullable()` — BE `BlType` 5상수(ORIGINAL/SURRENDER/SEAWAY/NORMAL/EXPRESS)와 정합, ComboBox 옵션은 `useEnumOptions("BlType")`로 동적 fetch라 strict 검증 불필요. ⑥ containers/desc nested 위치 정합 — Frontend-coder 1차 작업에서 `HouseBlDetail` 본체에 추가했으나 BE는 `seaDetail` nested 노출 → `HouseBlDetail.seaDetail.containers/desc` + `SEA_DETAIL_SCHEMA` 안으로 이동, `map-house-bl-detail`도 `detail.seaDetail?.containers/desc` 경로로 정합. ⑦ Container Type ComboBox label `o.value` → `o.label` (BE 변경 후 label=getCode 짧은 코드 표시, Sea House + Truck B/L 동시). **(04a8127 — 2 fix)** ⑧ `HouseBlDetailResult`/`HouseBlDetailResponse`에 `mblNo`/`settlePartnerCode` 노출 (Create/Update Request·Assembler엔 있었으나 detail 미노출 — Summary는 settlePartnerCode 노출). ⑨ FE `map-house-bl-detail` 의 `mbl: String(detail.masterBlId)` → `mbl: detail.mblNo`(BL 번호 표시) + `masterBlId` hidden 매핑 분리(submit PK 전달용) + `settlePartnerCode` 매핑 추가. **(추가 작업)** ⑩ `house-bl-entry.tsx` 500→410줄 분리 — fix 누적 후 ARCH2 500줄 강제 분리 발동, `form.reset` 매핑 100줄 블록을 `map-house-bl-detail.ts` 107줄로 추출. **(검증 함정)** dev 환경에서 BE 변경 후 spring-boot 재기동 안 하면 옛 enum 응답이 staleTime:Infinity로 캐시되어 FE 매핑 변경 결과가 description 표시처럼 보이는 일시적 mismatch 발생 — 사용자 검증 안내에 BE bootRun 재시작 명시 의무. **§6.49 매트릭스 ⑬~⑰ 신규 5건 등재** — 차기 도메인 사전 점검 의무 (BE Request DTO↔DetailResponse 정합 / EnumRegistryFactory value SSOT / zod .optional() null 처리 / form key 4축 정합 / strict enum 회피).

- **Sea House (sea-imp/sea-exp) FE↔BE 결합 미흡 부분 5건 일괄 정리 (2026-05-13)** — `docs/migration/` §6.48·§6.49·§13.5 매트릭스 점검 결과 잔존 결함 5건 해소. **(BE)** ① `SeaDetailProjection`/`SeaDetailResponse`에 `containers`(List)·`desc`(marks/description/descClause1/descClause2) 필드 추가 + nested record `SeaContainerProjection`/`SeaDescProjection`/`SeaContainerView`/`SeaDescView` 신설, `HouseBlFactory.toDetailResult`에 `toSeaContainerProjections`/`toSeaDescProjection` 헬퍼 추가(295→338줄, 300줄 초과 분리 검토 대상). ② `POST /api/house-bl/find-by-hbl-no` EXACT PK endpoint 신설 — `FindHouseBlByHblNoRequest`+`HouseBlUseCase.findHouseBlKeysByHblNoExact`+`HouseBlPort`+`HouseBlRepositoryImpl` QueryDSL(`jobDiv` AND `hblNo.value` EXACT, `orderBy(createdAt DESC, id DESC)` secondary key로 T1 결정론 보장, limit 2). 신규 테스트 3종(`SeaHouseDetailProjectionTest` 4케이스, `HouseBlControllerFindByHblNoTest` 4케이스, `HouseBlFindByHblNoIntegrationTest` 7케이스). **(FE)** ③ `HouseBlDetail`에 `containers`/`desc` 필드 + `HouseBlSeaContainerView`/`HouseBlSeaDescView` 인터페이스, `HOUSE_BL_DETAIL_SCHEMA` zod 동기(§6.29), `form.reset`에 containers·desc 매핑 추가. ④ `licenses` 미사용 도메인 흔적 FE 5파일·docs 1파일 전수 제거 — `domain/house-bl/index.ts` `LicenseRequest`+필드, `house-bl-schema.ts` `LICENSE_SCHEMA`+필드, `house-bl-defaults.ts`, `house-bl-submit.ts` SEA EXP 분기 블록, `panels/license-panel.tsx` 파일 삭제(BE는 `house_bl_license` 별도 제거로 이미 0건). ⑤ `deleteMutation.onSuccess`에서 `['house-bl','list']` 자동 invalidate 제거(§6.21 준수). ⑥ Delete 버튼 `window.confirm` → 공용 `@/components/confirm` 모달 전환(Non B/L 16dbc0b 패턴, §959). ⑦ `useSearchBl` 훅 LIKE+COUNT 재사용 → `houseBlPort.findByHblNo` EXACT 호출로 전환(§6.19), 0건/다건/단건 분기. **(잔존 open)** AIR/TRUCK 자식 컬렉션(scheduleLegs/airCharges/dims/truckOrders) BE detail 응답 노출은 별도 작업, `house-bl-entry.tsx` 500줄 경계(form.reset 약 100줄 별도 mapper 분리 권장), `master-bl-entry.tsx`/`switch-bl-modal.tsx` `window.confirm` 잔존은 후속 일괄.

- **Sea House Entry Change B/L No invalidate 중복 + toolbar HBL No 미갱신 fix (2026-05-14, 0b8a2ee)** — `house-bl-entry.tsx`의 `HouseChangeBlNoModal` `onChanged` 콜백에서 `queryClient.invalidateQueries(["house-bl","detail",id])` 호출 제거(모달이 이미 같은 queryKey invalidate 중). 부모 콜백 중복 호출이 fetch 2회 발사 → BE detail SELECT 4×2=8건 발생 + 1차 fetch가 `detailLoadedRef.current=true`(직전 detail 로드 후 §6.49 ⑨ 가드 설정 상태)에서 시작되어 useEffect form.reset 가드(`if (detailLoadedRef.current) return;`)에 막혀 새 hblNo로 form.reset이 스킵되는 race 동시 발생. 부모(Entry)는 `detailLoadedRef.current=false`로 form.reset 재발동만 책임. SELECT 8→4 절반 절감. 잔여 점검 권장 — 다른 Entry의 `*ChangeBlNoModal onChanged` 콜백 중복 invalidate 점검.

- **House B/L Entry toolbar Master Ref 바인딩 누락 풀스택 정합 (2026-05-14, 77341a8)** — Sea House Entry toolbar "Master Ref" 필드(`masterRefNo`)가 항상 빈 값으로 표시되던 §6.49 ⑧ 결함. 저장 경로(Create/Update Command·Request·Assembler·`HouseBl.assignMasterReference`·`HouseBlDomainToJpaMapper.setMasterRefNo`·`HouseBlJpaToDomainMapper.assignMasterReference` + DB `master_ref_no` 컬럼)는 정상이었으나 **detail 조회 응답 경로 5축이 비어 있음**. 풀스택 5파일 동시 정합: (BE) `HouseBlDetailResult`/`HouseBlFactory.toDetailResult`/`HouseBlDetailResponse` record + factory. (FE) `domain/house-bl/index.ts` `HouseBlDetail` interface + `adapter/out/api/house-bl.ts` `HOUSE_BL_DETAIL_SCHEMA` zod 파싱 라인(누락이 결정적 원인 — BE가 보내도 zod 파싱 시점에 소실) + `map-house-bl-detail.ts` `detail.masterRefNo → form.masterRefNo`. **SSOT 학습** — FE 도메인 타입과 zod adapter schema는 1:1 정합. §6.49 ⑧ 점검 항목에 "FE adapter zod schema(`adapter/out/api/<domain>.ts` `*_DETAIL_SCHEMA`)에도 신규 필드 grep" 의무 추가.

- **ServiceTerm enum 매칭 비대칭 — Sea/Master fromLabel → valueOf 통일 (2026-05-14, 7f91282)** — Sea House Entry/Master B/L Entry에서 Service Term 포함해 Save 시 `IllegalArgumentException: Unknown service term code: CFS_CFS` 발생. `EnumRegistryFactory` SSOT가 `value=e.name()`("CFS_CFS"), `label=e.getLabel()`("CFS/CFS")로 노출하므로 FE form은 name 값 저장 후 update Command 전송 → 그러나 BE 호출처 일부(`HouseBlSeaSubFactory:74`, `MasterBlFactory:203`)가 `ServiceTerm::fromLabel`(label만 매칭) 사용. Truck 측(`HouseBlTruckSubFactory:32, 45`)은 이미 `ServiceTerm::valueOf` 사용 중. fix: Sea/Master 2곳을 `ServiceTerm::valueOf`로 통일(Truck SSOT 정합). `ServiceTerm.fromLabel` 본체는 외부 호환 목적으로 enum에 보존(현재 호출처 0건). `HouseBlSeaSubFactoryTest:138` 헬퍼 입력 `"CY/CY"`(label) → `"CY_CY"`(name)으로 production 정합(사용자 명시 승인). **SSOT 학습** — enum 매칭 함수 일관성: `EnumRegistryFactory`의 value 정의(`e.name()` vs `e.getCode()`)와 호출처 변환 함수(`valueOf` vs `fromCode` vs `fromLabel`) 1:1 정합 의무.

- **House B/L update SEA void 응답 파싱 — @JsonInclude(NON_NULL) 정합 (2026-05-14, dd76d64)** — Sea House Entry Save 시 화면에 `Invalid update response: ... expected object, received undefined` 빨간 박스 발생(직전 ServiceTerm fix로 update가 BE 단에서 성공하게 되자 응답 파싱 결함이 표면화). 원인: BE `ApiResponse` 클래스 `@JsonInclude(JsonInclude.Include.NON_NULL)`이 SEA jobDiv update의 `ApiResponse.ok(message)`(`data=null`) 직렬화 시 `data` 키 자체를 응답에서 제거 → 실제 응답 `{"message": "..."}`. FE adapter는 `z.object({ data: z.null() })`로 void 검사하지만 zod 입장에서 `data: undefined`는 `z.null()` 거부 → maybeVoid 실패 → 다음 줄 detail 파싱으로 폴백 throw. fix: `house-bl.ts:184` maybeVoid 검사를 `(json ?? {}).data == null`로 완화(null/undefined 모두 void 처리). BE `@JsonInclude(NON_NULL)`은 다른 응답들에 폭넓게 걸려 있어 건드리지 않음. **SSOT 학습** — BE `@JsonInclude(NON_NULL)` 정책 하에서는 null 필드가 응답에서 사라짐을 전제로 FE adapter 작성. zod `z.null()`은 null만 매칭하므로 undefined 호환 위해 `data == null` 또는 `z.null().optional()` 사용. 잔여 점검 권장 — `non-bl.ts`/`truck-bl.ts`/`master-bl.ts` 등 다른 adapter도 같은 패턴 점검.

- **Sea House Entry Create 필수 검증 EXP/IMP 분기 — Bean Validation Groups 도입 (2026-05-14, 69271ea)** — Sea House Entry 신규 저장 시 BE 필수값 검증이 hblNo/polCode/podCode/etd/eta/actualCustomerCode/docPartnerCode/operatorCode/teamCode 9개 + shipmentType/bound 2개 = 11개 필드에 누락. UI required 시각 표시도 사용자 명시 목록(공통 10 + IMP Consignee 1)과 불일치한 잉여 6건(Vessel/Voyage/Liner/Freight Term/Incoterms 등) 정리 동반. (BE 5파일) `adapter/in/web/validation/SeaGroup.java`/`SeaImpGroup.java` 신규 — `SeaImpGroup extends SeaGroup`으로 IMP 검증 시 공통 그룹 함께 검증. `CreateHouseBlRequest`에 공통 10개 `@NotBlank(groups=SeaGroup.class)` + Consignee `@NotBlank(groups=SeaImpGroup.class)` 부착. `HouseBlController.createHouseBl`에 `jakarta.validation.Validator` 빈 주입 + `jobDiv=SEA` 진입 시 bound 별 동적 그룹 검증, 위반 시 `ConstraintViolationException` throw. `GlobalExceptionHandler.handleConstraintViolation` 신설(기존 `MethodArgumentNotValidException` 핸들러와 동일 ProblemDetail 응답 패턴). `HouseBlControllerWebMvcTest.createHouseBl_happyPath` 요청 body에 SEA+EXP 필수 9개 더미 값 보강(사용자 명시 승인). (FE 3파일) `schedule-panel.tsx` Vessel/Voyage/Liner required 제거(ETD/ETA/POL/POD 유지), `sea-trade-panel.tsx`/`trade-panel.tsx` Incoterms/Freight Term required 제거. Consignee 동적 IMP-required는 `party-panel.tsx` + `widget-grid.tsx` `isExp` prop chain으로 기존 구현 활용. 메모리 `feedback_validation_be_ssot` 정합 — FE에 zodResolver/HTML5 required 추가 안 함(시각 클래스만 정합). 잔여 점검 권장 — Air/Truck House Entry, Sea/Air Master Entry에 동일 패턴 적용은 별도 작업.

- **Sea House Entry Save round-trip 3건 정합 — mblNo/linerCode/deliveryCode (2026-05-14)** — 사용자 회귀 보고 3종 fix. (1) **mblNo update null**: toolbar form 키 `mbl`(entry.tsx:85 `"MBL No": "mbl"`)에 값이 있으나 `house-bl-submit.ts:59`가 `values.mblNo`(undefined) 읽음 → BE에 null 전송. fix: `values.mbl`로 변경. (2) **linerCode update 미발사 + 조회 미바인딩**: SEA의 linerCode는 BE Request에서 `SeaDetailRequest.linerCode`(nested)에만 존재(본체 linerCode는 Non B/L 전용)인데 `schedule-panel.tsx:188` `register("linerCode")`는 본체 키 등록 → submit 시 seaDetail.linerCode 빈 채 BE 전송 → BE applySeaUpdate가 null 받아 기존값 유지(PATCH 의미론) + 조회 시 register 안 된 nested 키라 화면 미표시. fix: `register("seaDetail.linerCode")`로 정정. linerName은 마스터 데이터 테이블 도입 시 후속 처리(사용자 지시). (3) **deliveryCode update 미발사 + 조회 미바인딩**: `HouseBl` 본체 필드인 deliveryCode가 BE Request/Command에서는 `SeaDetailRequest/Command.deliveryCode`(nested)로 전달. `HouseBlFactory.applyCreate:118`은 정상 매핑(`PortCode.of(cmd.seaDetail().deliveryCode())` → entity.assignDeliveryCode)하나, **`HouseBlSeaSubFactory.applySeaUpdate`(45-68)에 deliveryCode PATCH 라인 자체가 누락**되어 사용자 변경값이 DB UPDATE에 미반영 → round-trip 침묵 실패. fix: applySeaUpdate 끝에 `if (s.deliveryCode() != null) entity.assignDeliveryCode(PortCode.of(s.deliveryCode()));` 1줄 추가 + 회귀 테스트 `applySeaUpdate_deliveryCode_patches` 신설. **SSOT 학습** — phase1-setup.md §6.49 ⑯에 (a) form 키 `mbl` vs submit `values.mblNo` 사례 (b) BE Request 본체 vs nested 위치 정합 의무 추가. §6.49에 **⑱ BE create↔update apply 분기 PATCH 누락 비대칭** 신설 — `apply{Mode}Create` 처리 필드 set ⊆ `apply{Mode}Update` 처리 필드 set 정합 의무 + 사용자 검수에 "신규 저장+Update+재조회 round-trip" 명시 의무. **잔여 점검 권장** — Air/Truck House Entry, Sea/Air Master Entry에 동일 apply create↔update 비대칭 점검 별도 plan.

- **House B/L Entry `searchParams.id` 동기 effect 제거 — F5 자동조회 회귀 fix (2026-05-14)** — Sea House Entry 한 번 조회 후 강력 새로고침(Ctrl+Shift+R) 시 이전 조회 데이터로 자동 조회되는 회귀. 원인: `house-bl-entry.tsx`에 URL `?id=` → `useEntryFocusStore.setFocus` 동기 effect가 잔재로 남아 store는 초기화되어도 URL ?id가 잔존하면 effect가 재실행 → `isEdit=true` → useQuery 발화. fix: `useSearchParams` import·변수·동기 effect 3종 일괄 제거(단순 삭제). 결과: id는 오직 `useEntryFocusStore.focus[houseBl(variant.key)]`에서만 읽고, store는 persist 없음(`use-entry-focus-store.ts`)이라 F5 시 초기화 → 빈 폼. Sea/Air Exp·Imp 4 variant 공통 적용. Truck/Non B/L Entry의 store-only 정책과 정합 완료. **§6.26 갱신** — 패턴 A(path-param)/패턴 B(query-param) 구분 폐기, store-only 단일 표준으로 단순화 + URL path-param 노출 안티패턴 등재(2026-05-09 fix 1482b65와 연결). **SSOT 학습** — Entry 화면은 URL에 id 노출 금지(URL/탭 이름 사이드 이펙트 회피) + `useSearchParams` 동기 effect 금지(F5 자동조회 차단). 새 도메인 Entry 추가 시 store-only 패턴 강제.

- **Sea House Entry §6.37 sub-set 매퍼 적용 — master 참조 NULL 덮어쓰기 방지 (2026-05-14)** — Sea House Entry 무수정 저장 시 `house_bl` UPDATE가 발사되어 `master_bl_id`/`mbl_no`/`master_ref_no` 3필드가 NULL로 덮어써지는 회귀. 근본 원인: `HouseBlFactory.applyToEntity`가 `assignMasterReference(MblNo.of(cmd.mblNo()), cmd.masterRefNo())`를 무조건 호출 → cmd가 null인 update 경로에서 도메인에 null 할당 → `HouseBlDomainToJpaMapper.applySeaCommonFields`가 그대로 setter 호출. fix: (BE 2파일) `applySeaCommonFields`에서 `setMasterBlId`/`setMblNo`/`setMasterRefNo` setter 3라인 제거(Truck 선례 8c54dd9 · Non B/L 선례 f15736e와 동일 sub-set 패턴), `HouseBlFactory.applyToEntity`의 `assignMasterReference` 호출 앞에 `if (cmd.mblNo() != null || cmd.masterRefNo() != null)` null 가드 추가. (테스트) `HouseBlDomainToJpaMapperSeaTest` 신설 — `setMasterBlId`/`setMblNo`/`setMasterRefNo` `never()` verify 3건. (FE 사전 작업 정정) 직전 commit `1c4f7e8`의 FE isDirty 가드는 회귀 차단 효과 미흡(컨테이너만 수정 시 동일 house_bl UPDATE 발사)이라 revert(`fa2ea78`). **SSOT 학습** — §6.37 sub-set 매퍼 패턴은 모든 House B/L 변형(Sea/Air/Truck/NonBl)에 적용 의무. master 참조(`masterBlId`/`mblNo`/`masterRefNo`)는 form 미보유 모드에서 setter 라인 자체 제거 필수, factory 호출 시 null 가드 동반. **분리 후속** — `HouseBlDomainToJpaMapper.java`(296줄) · `HouseBlFactory.java`(346줄) 300줄 임계 보고, 후속 분리 작업으로 이관. **잔여 점검 권장** — Air House Entry · Sea Master Entry · Air Master Entry에 동일 sub-set 패턴 적용은 별도 plan(form 미보유 필드 식별 + 어댑터 매퍼 분기 신설/setter 제거).

- **Sea House Entry INSERT NULL 저장 2건 + POST 응답 SELECT 2세트 중복 fix (2026-05-14)** — 사용자 보고 3종 fix. (1) **`load_type` NULL 저장**: `house-bl-entry.tsx:87` `TOOLBAR_LABEL_TO_FIELD["Load Type"]="lType"` (root 키)인데 `house-bl-submit.ts:69`가 `sd.loadType` (nested)만 BE에 보냄 → toolbar 입력이 BE에 전송 안 됨. fix: 매핑을 `"seaDetail.loadType"`로 정정. root `lType`은 dead field 잔존(read 0건 검증). (2) **`no_of_bl` NULL 저장**: BE 응답 path는 이미 enum.name() String 컨벤션(`SeaDetailResponse.noOfBl: String`, `HouseBlFactory:275 NoOfBl::name`)이나 요청 path만 `Integer noOfBl` 잔존(Request/Command record, `HouseBlSeaSubFactory:32, 58` `NoOfBl::fromNumber`). FE ComboBox value는 enum.name()("THREE")이라 `house-bl-submit.ts:78 toNum("THREE")=NaN→undefined` → BE null 전송. fix(BE): `CreateHouseBlRequest:84`/`UpdateHouseBlRequest:81`/`CreateHouseBlCommand:86`/`UpdateHouseBlCommand:86` 모두 `Integer→String`, `HouseBlSeaSubFactory:32 NoOfBl.fromNumber(...)→Nullables.mapOrNull(..., NoOfBl::valueOf)`, `:58 NoOfBl::fromNumber→NoOfBl::valueOf`. 테스트 픽스처 `HouseBlSeaSubFactoryTest:148` 인자 `3→"THREE"` (사용자 명시 승인). fix(FE): `submit.ts:78 toNum→toStr`, `domain/house-bl/index.ts:138 noOfBl?: number→string`. `NoOfBl.fromNumber` 메서드는 다른 호출처 없음이지만 유지(회귀 위험 0, 사용자 결정). 메모리 [feedback_enum_db_value] 정합. (3) **INSERT 후 SELECT 2세트(=4×2) 중복**: 1차 진단으로 FE `mutation.onSuccess` CREATE 분기의 `queryClient.invalidateQueries({queryKey: ["house-bl","detail",newId]})` 1줄 제거 시도 → 효과 없음 (사용자 재보고). 진짜 원인은 `HouseBlController.createHouseBl:66-77`이 응답을 만들 때 `houseBlAssembler.toDetail(houseBlUseCase.findHouseBlById(id))` 호출 → POST 트랜잭션 내부에서 detail SELECT 4건 추가 발사. NonBl(608d0df, 2026-05-10) + TruckBl가 이미 `ApiResponse<Map<String, Long>>` + `Map.of("id", id)` 패턴으로 해결되어 있었으나 HouseBl만 미정렬 잔존. fix(BE): `HouseBlController.java` POST 메서드 시그니처 `ApiResponse<HouseBlDetailResponse>→ApiResponse<Map<String, Long>>`, body `assembler.toDetail(useCase.findById(id))→Map.of("id", id)`, `java.util.Map` import 추가. fix(FE): `application/house-bl/ports.ts:7 create(): Promise<HouseBlDetail>→Promise<{id: number}>`, `adapter/out/api/house-bl.ts:165-174 apiResponse(HOUSE_BL_DETAIL_SCHEMA)→apiResponse(z.object({ id: z.number() }))`, `adapter/out/mock/house-bl.ts:47-49 create 반환 {id: 1}` + 사용 안 하는 `req→_req` prefix, CREATE 분기 `invalidateQueries` 함께 제거(`house-bl-entry.tsx:174-175`). fix(테스트, 사용자 승인): `HouseBlControllerWebMvcTest.createHouseBl_happyPath_returns201WithLocation`의 죽은 stub(`mockSavedResult`/`mockResponse`/`findHouseBlById`/`toDetail`) 4줄 제거 + `findHouseBlById` 호출 검증 제거 + `.andExpect(jsonPath("$.data.id").value(1))` 응답 본문 검증 추가. **결과**: INSERT 후 사용자 관점 SELECT 8건(2세트) → 4건(1세트) 절감, `load_type`/`no_of_bl` 정상 저장. **SSOT 학습** — phase1-setup.md §6.49에 **⑲ BE Controller POST 응답 detail 재조회 안티패턴(SELECT N×2)** 매트릭스 신설 + 체크리스트 17(BE Controller POST 응답 ID-only 컨벤션) · 18(enum 요청/응답 path 타입 대칭) 신설, ⑯에 (c)(d) 사례 추가. phase3-data-flow.md **§6.54 BE POST 응답 ID-only 컨벤션** SSOT 본문 신설. 메모리 [feedback_get_dup_diagnosis] 신설 — INSERT/UPDATE 후 SELECT N×2 증상 보고 시 **BE controller POST 응답 빌드 패턴(`findXxxById`/`toDetail` 호출)을 1순위로 grep, FE invalidate cascade는 2순위**. **잔여 점검 권장** — `MasterBlController.createMasterBl`·기타 `<Domain>Controller.create<Domain>` 메서드 응답 시그니처 grep 의무, `ApiResponse<<XxxDetail>Response>` 잔존 시 동일 정렬.

- **2026-05-14 세션 정리 — Sea House Entry 마무리 + Truck B/L desc 정합 + 신규 SSOT 5건 (Air House 진입 전 의무 적용)** — 본 세션 18 commits 가운데 마이그레이션 패턴화가 가능한 SSOT 5건을 phase3-data-flow.md §6.55~§6.59에 신설. **(§6.55 Detail 응답 nested object 필드 null → `empty()` 정적 팩토리, 1cc4375)** Truck B/L `desc` 필드(`marks`/`description`/`descClause1`/`descClause2`)가 BE에서 null로 직렬화되어 FE zod object 스키마 `Invalid input: expected object, received null` 토출. fix: `TruckBlDetailResult.DescView`/`TruckBlDetailResponse.DescView` 양쪽 record에 `empty()` 정적 팩토리(모든 필드 null) 추가 + `TruckBlFactory.toDescView` + `TruckBlDetailResponse.from` 양 경로에 null → `DescView.empty()` 분기. `TruckBlFactoryTest.toDetailResult_nullDesc_returnsNullDescView` → `*_returnsEmptyDescView` + `isEqualTo(DescView.empty())` 단언 갱신(사용자 승인). **(§6.56 useFieldArray race swipe — `resetVersion` key prop 버저닝, 66d47b6)** Cargo 패널이 `useFieldArray`로 RHF state 보유 중 부모 `form.reset`/`replace([])` 호출 시 internal id 캐시와 React reconciler race로 stale 행 swipe. fix: 부모 entry에 `const [resetVersion, setResetVersion] = useState(0)` + 자식 컴포넌트 root에 `<CargoPanel key={resetVersion} ... />` + Delete/New/Save 핸들러에서 `setResetVersion(v=>v+1)`. Sea/Non B/L/Truck 3도메인 동시 적용. **(§6.57 Entry Delete/New 후 자식 탭 stale 표시 race 차단, 5b36ea6)** Cargo 탭이 별도 `useQuery`로 조회되는 구조에서 Delete/New 트리거 후 invalidate-refetch 사이클 동안 이전 row 표시. fix: Delete/New 핸들러에서 자식 grid local state 즉시 초기화(`setCargoRows([])`) + `form.reset(emptyDefaults)` 동시 호출. Sea House + Non B/L Cargo 적용. **(§6.58 Entry onSave 가드 `!isEdit` 신규 저장 차단 안티패턴, 41198a4)** Non B/L Entry onSave에 `!isEdit` 가드 잔존으로 신규 모드 Save 시 `createNonBl` mutation 무발사(UI 무반응). 가드 제거 후 정상. edit 전용 후처리에만 가드 적용하는 패턴이 SSOT. **(§6.59 ComboBox label 매핑 정합 — `useEnumOptions` value/label 시각 검증 의무, 204ac4c)** Non B/L Container Information ComboBox label이 enum.name() 또는 swap된 채 표시되는 회귀. lint/build PASS만으로 검출 불가 → dev preview catalog 시각 확인 의무(메모리 [feedback_fe_verify_before_commit] 정합). **그 외 본 세션 commits (history 이미 등재 또는 SSOT 비파생)**: 27001b8(F5 자동조회 회복 + 더블클릭 회귀 + Save round-trip 3건) · 69271ea(Sea House Create EXP/IMP 분기 + UI required 정합) · dd76d64(House B/L update SEA void 응답 파싱 @JsonInclude NON_NULL 정합) · 7f91282(ServiceTerm 매칭 `fromLabel` → `valueOf` 통일 Sea/Master) · 77341a8(toolbar Master Ref 바인딩 detail 응답 5축 풀스택 정합) · 0b8a2ee(Change B/L No invalidate 중복 제거 + toolbar HBL No 미갱신 race fix) · a04f066(Sea House §6.37 sub-set 매퍼 master 참조 NULL 덮어쓰기 방지) · f05dc19(Sea House INSERT NULL 저장 2건 + POST 응답 SELECT 2세트 중복) · dab8e95(Sea House Save 확인 모달 적용, Truck Entry 패턴 재사용) · 326d26b(Sea House 컨테이너 SEAL NO. 2/3 컬럼 추가) · 1c4f7e8 → fa2ea78(무수정 Save 차단 도입 후 revert — 회귀 차단 효과 미흡, §6.37 매퍼 fix로 대체) · e830f97(Truck B/L containerType detail 응답 매핑 §6.45 후행 정합) · 27790e0(Non B/L containerType detail 응답 매핑 §6.45 후행 정합). **문서 갱신** — phase3-data-flow.md §6.55~§6.59 본문 5건, phase1-setup.md §13.6 권장 진행 순서(Sea House **완료** / **Air House가 다음 타깃**) · §13.7 Air House 진입 전 의무 점검 체크리스트 신설(§6.55~§6.59 매트릭스 + Sea House 잔여 적용 잠재 항목 6건). **잔여 점검 권장** — Air House Entry 마이그레이션 plan 단계에 phase1-setup.md §13.7 표를 의무 grep 체크리스트로 인용.

- **Air House Entry 마이그레이션 완료 (2026-05-14, Phase 0~9 완료)** — Sea House Entry(2026-05-14 완료)에 이어 두 번째 House B/L 변종 마이그레이션. 9 Phase 분할 진행, §6.48 7항목 + §6.49 16항목 + §6.53/§6.54/§6.55~§6.59 신설 SSOT 22건 전수 적용. **Phase 0** (`f392f5e`): 사전 grep 매트릭스 + `AirGroup`/`AirImpGroup` marker interface 신설. **Phase 1** (`97759be`): `AirDetailRequest`/`AirDetailCommand` nested record + Assembler 호출처 정합. **Phase 2** (`32e5f54`): `AirDetailProjection`/`AirDetailResponse` + `HouseBlDetailResponse.airDetail` 노출, `DescView.empty()` §6.55 적용. **Phase 3** (`163cc37`): Controller AIR 분기 동적 검증 + §6.54 ID-only 응답 정합. **Phase 4** (`a0a6b2c`): `AirBlUpdatePersistenceAdapter` 신설 + sub-set 매퍼 §6.37 적용, 20필드 매핑 완성. **Phase 5** (`8fd38d6`): FE adapter zod schema(airDetail nested) + domain type + `map-house-bl-detail.ts` AIR 분기(root → nested 이전). **Phase 6** (`ff584ec`): 6개 패널 카탈로그 컴포넌트 교체 + rowKey 정정 + dev preview catalog 6 enum 섹션 추가 + `air-issue-panel.tsx` `seaDetail.*` 오기입 `airDetail.*` 정정. **Phase 7** (수정 0건 — 검증만): `key={resetVersion}`이 상위 `MainTabAir` 레벨에 이미 적용, §6.56~§6.58 전항목 정합 확인. **Phase 8** (`bcbcf70`): Air House Entry bundle 카탈로그 추가, toolbar 후보 4필드(Bound/Service Term/Flight Type/Movement Mode) 전원 BE 미지원 확정 — toolbar는 기존 root 4필드(`sType`/`hbl`/`mbl`/`masterRefNo`)로 완결. **Phase 9** (본 Phase): E2E 회귀 4종 스펙 신설 + docs 갱신. **주요 product 결정**: House Air BE toolbar 후보 4필드 전원 미지원 — `FlightType`/`ServiceTerm`/`MovementMode` BE 도메인 확장은 별도 백로그. **주요 발견 패턴**: Phase 5→6 이전 과정에서 매핑 상수 객체(`AIR_ISSUE_LABEL_TO_FIELD`)에 `seaDetail.*` 도메인 잔존 오기입 발견 → §6.60 후보(차기 도메인에서 신설 검토). **§13.7 해소 선언**: Air House 진입 전 의무 점검 체크리스트(§6.55~§6.59) 5항목 전수 적용 완료. 차기 타깃: Sea Master Entry (§13.6 순서 ③).

- **Sea Master Entry 마이그레이션 완료 (2026-05-15, Phase 0~9 완료)** — Sea House(2026-05-14) → Air House(2026-05-14) 다음 세 번째 도메인 마이그레이션. 9 Phase 분할 진행, §6.48 7항목 + §6.49 16항목 + §6.55~§6.59 의무 점검 + §6.61 신설(WebMvcTest 동적 Validator 빈 mock 금지) 전수 적용. **Phase 0** (`3df084a`): `SeaMasterGroup`/`SeaImpMasterGroup` marker interface 신설 + 사전 grep 매트릭스. **Phase 1 SKIPPED**: 원 plan 항목(CustomerCode VO / address 컬럼 / nested record / remark 위치) grep 결과 모두 완료 상태로 발견, 추가 작업 0건. **Phase 2** (`8ca8ca3`): `SeaDetailProjection`/`SeaDescProjection` + `MasterBlDetailResponse.SeaDetailResponse`/`SeaDescView.empty()` §6.55 + address 3 필드 본체 노출. **Phase 3** (`adc2f3d`): Controller SEA 분기 동적 검증 + §6.54 ID-only 응답 + EXACT endpoint `find-by-mbl-no` + `shipmentType`/`teamCode` 누락 필드 보강. 회복 라운드 학습 → §6.61 신설: WebMvcTest `@MockitoBean Validator` 추가 시 `@Validated`+`MethodValidationInterceptor` NPE 회귀 (1차 14건 FAIL → 2차 검증 우회 3건 → 3차 PASS). **Phase 4** (`cb30351`): `SeaMasterPersistencePort` + `SeaMasterUpdatePersistenceAdapter` 신설 + `MasterBlSeaSubMapper` 추출 (3분기 분류 §6.49 ㉓) + `assignParties` null 가드 (§6.37 PATCH 의미론) + 신규 테스트 14 케이스. **Phase 5** (`3d6016a`): FE adapter zod `seaDetail` nested + `MasterBlSeaDetail` interface + `freightTerm` enum→`string|null` 완화 §6.49 ⑰ + `map-master-bl-detail.ts`/`master-bl-submit.ts`/`master-bl-defaults.ts` 분리 + `Status` 필드 제거 + `master-bl-entry.tsx` 313→248줄. **Phase 6** (`a0d4882`): `main-sea.tsx`/`main-air.tsx` registry 분리 + 패널 4종 카탈로그 컴포넌트 일괄 교체 (native input 40+ 제거, CodeBox lcn 1/code-only 4/party-cn 3, ComboBox+useEnumOptions 5, LineNumberTextarea 6, DateBox 4) + inline style 104→42건 + dev preview catalog 12 컴포넌트 노출 + `master-schedule-sea-fields.tsx`/`master-schedule-sea-atoms.tsx` 신설. **Phase 7** (`747cf48`): `useState resetVersion` + `<MasterMainTab key={resetVersion}>` + `<FreightTab key={resetVersion}>` 부착 + `handleResetEntry`/`deleteMutation.onSuccess`에 `setResetVersion(v=>v+1)` + `detailLoadedRef.current=false` (§6.56/§6.57 House 패턴 정합). **Phase 8** (`5ffcd3e`): toolbar 7 필드 카탈로그 교체 (TextBox 3 + ComboBox+useEnumOptions 4) + TOOLBAR_TO_FIELD 4축 정합 root vs `seaDetail.*` nested (§6.49 ⑯ c) + page-head Button 카탈로그 6 + window.confirm 모달 전환 2 경로 (Non B/L `16dbc0b` 패턴) + useBlDraftSync `didRestoreFromDraftRef` 가드 (§6.49 ⑨) + mutation onSuccess SSOT 5요소 정렬 (§6.49 ⑩: invalidate detail / sessionStorage hot / setFocus / clearDraft / detailLoadedRef=false, List 자동 invalidate 0건, router.push 0건) + `entryFocusKeys.masterBl` 헬퍼 신설 + master-bl-entry 257→332줄. **Phase 9** (본 Phase): E2E 회귀 4종 spec 신설 + docs 갱신 (history.md / phase1-setup.md §13.6+§13.8 / phase3-data-flow.md §6.61). **주요 product 결정**: `incoterms`/`deliveryCode` Master 도메인 미적용 (Sea House `HouseBl.deliveryCode`와 PRD 차이) / `remark` SEA/AIR 확장 분산 유지 / Toolbar `Status` FE 제거 / AIR Master 코드 격리 원칙 (`MasterBlAir*`/AIR 분기 0건 수정). **주요 발견 패턴**: WebMvcTest 동적 Validator 빈 mock 금지 (§6.61). **§13.6 ③ 해소**: Sea Master Entry 완료. 차기 타깃: Air Master Entry (§13.6 ④). **누적 후속 (별도 처리)**: master-bl-entry.tsx 332줄 분리 검토 / AIR Master 분기 schema 미정의 필드(`seaDetail.linerName`/`polName`/`signature`/`issuePlace`) Air Master Phase 처리 / `non-bl/__tests__/map-non-bl-detail.test.ts` `jobDiv` 기존 회귀 별도 처리 / `master-cargo-doc-panel.tsx` warning 2건 (`Controller` import / `form` 파라미터 미사용).

- **Air House Entry 화면 검수 후속 1라운드 — 패널/그리드 디테일 정합 14건 (2026-05-14 20:13~22:24)** — Phase 0~9 완료 후 사용자 화면 검수에서 누적 발견된 14건 일괄 정리. **(라벨/필드 정리)** 99eaa70 Performance 패널 중복 필드 3건 제거 / a2e6d7a Schedule 라벨 `Carrier → Airline` 통일 / e0cbe8d Entry tabbar 정적 "Last saved" 표시 제거 / 87ca4dc Schedule 필수 필드 5종(`hblNo`/`polCode`/`podCode`/`etd`/`eta`) 시각 마커 복원 / 8408b2f Handling Information 패널 Code 라벨 제거 / 863519e Trade 패널 Other Term / F.H.D를 `<select>`에서 `ComboBox` + `useEnumOptions("FreightTerm")`/`useEnumOptions("Fhd")`로 교체. **(패널 배치)** 7376b51 Cargo 패널 Package CodeBox 80px 폭 적용(`air-cargo-panel` scope CSS) / 24c7079 Charge Information 그리드 컬럼 너비 조정 / fb18845 Document·Performance·Cargo 패널 배치 EXP·IMP 공통화 / 31d51f8 Party·Performance 패널 rowSpan 1행 축소(밀도 개선). **(폼 매핑 정합)** b3b6627 12개 필드 register/Controller name을 nested `airDetail.*` 경로로 통일(root 별도 schema 필드를 호출해 조회 시 빈 셀 렌더되던 결함 해소) / d898182 `map-house-bl-detail.ts` desc 블록 4필드에 AIR 우선 fallback(`airDetail?.desc?.* ?? seaDetail?.desc?.* ?? ""`) 추가 + `nature-goods-panel`의 root `natureOfGoods` → `desc.description` 통일 / da56a99 Schedule Legs 그리드 4개 dt/tm 셀 바인딩 fix + To/By/Flight 컬럼 너비 확장 / 29899d5 Charge Information Total Amount 셀 표시값 `toString()` → `toFixed(2)`(부동소수점 표시 2자리 통일).

- **Sea House Entry 패널 후속 정합화 + Cargo Package 80px (2026-05-14 21:34~21:35)** — Sea House Entry 풀 마이그레이션 후속 사용자 검수 라운드 2건. 918740d Schedule/Trade & Performance/Document 패널 필드 배치 조정 / f992ffe Cargo Package CodeBox 80px 폭 적용. forms.css의 `.lcn` 기본 grid(110px 120px minmax(0,1fr))가 wrapper `flex:0 0 80px`를 무시하던 문제를 panel 컨테이너에 `sea-cargo-panel` scope class 부여 + `widgets.css`에 `.sea-cargo-panel .li__input--tight > div > .lcn { display: block; width: 100%; padding: 0; gap: 0; }` 한 줄 추가로 grid 해제. truck/air-cargo-panel 선례와 동일 패턴(SSOT 차용).

- **Air House Entry BE/FE 결합 결함 정합 시리즈 (2026-05-14 23:30~23:31)** — Phase 0~9 마이그레이션 완료 후 사용자 회귀 보고 4건 일괄 정정. **(43fd342 — handlingInformation 분리 + 자식 그리드 id 흐름 복원)** ① `handlingInformation` 단일 String을 `handlingInformationCode`(HandlingInfoCode enum) / `handlingInformationDesc`(자유 텍스트) 두 필드로 분리 — FE submit이 code 값만 단일 필드로 보내고 BE Factory가 그것을 description으로 저장해 `handling_info_text`에 'A' 같은 code 문자열이 들어가고 `handling_info_code`는 NULL로 남던 비대칭 해소. BE Request DTO·Command·Assembler·SubFactory 갱신, `applyAirUpdate`는 `buildHandlingInformation` helper로 code/desc 각각 PATCH 의미론 유지. ② AIR 자식 그리드(Dim·Charge·Schedule) UPDATE 경로 id 흐름 복원: ScheduleLeg는 `HouseBlDocMapper.toScheduleLegJpa`에 `setHouseBlScheduleLegId` 호출 누락이 단일 결함이라 한 줄 추가. AirCharge는 Request/Command/Assembler/SubFactory/Mapper/JpaEntity 전 계층에 id 추가 + `HouseBlAirChargeJpaEntity.setHouseBlAirChargeId` setter 신규. Dim은 `UpdateHouseBlRequest.DimRequest`에 `Long id` 첫 필드 추가 + Assembler에서 `r.id()` 전달. `NonBlAssembler.toAirChargeCommandsU`는 시그니처 변경 컴파일 정합용 null 첫 인자 추가. **(11ca713 — Charge decimalPlaces)** `air-charge-info-panel.tsx`의 `grossWeightKg`/`chargeWeightKg`/`rate` 세 셀에 `decimalPlaces` prop이 누락되어 NumberBox가 `.` 키 입력과 소수 paste를 차단하던 문제 해소. SSOT(`grid-preview-panel`/`container-grid-panel`/`truck-order-grid-panel`) 패턴 따라 W/T 두 컬럼은 `decimalPlaces={3}`, rate는 `decimalPlaces={2}` 적용. **(6a9f7c7 — sub-set 매퍼 mblNo/masterRefNo conditional setter)** §6.37 sub-set 매퍼 4개(applyAirCommonFields/applySeaCommonFields/applyTruckCommonFields/applyNonBlCommonFields)가 의도적으로 `setMblNo`/`setMasterRefNo`를 호출하지 않아 form에서 두 필드만 수정해 저장하면 도메인까지는 값이 들어가지만 JPA dirty가 발생하지 않아 UPDATE 쿼리 자체가 안 나가던 버그 수정. `domain.getMblNo()`/`getMasterRefNo()`가 null이면 setter skip(DB 기존 값 보호, PATCH 의미론), 값이 있으면 set하여 dirty 발생하도록 conditional setter 추가. `masterBlId`는 별도 흐름(`linkToMaster`) 경유라 그대로, INSERT 경로(`applyCommonFields`)는 이미 정상 호출 중이라 미변경. `HouseBlDomainToJpaMapperSeaTest`의 setter "절대 미호출" 검증 2 케이스를 conditional 검증 4 케이스로 재작성(사용자 승인). **(d92525e — AIR Update Adapter desc sync 누락 보정)** UPDATE 경로가 `toAirDomain`에 `descJpa=null`을 넘기고 update 후에도 desc 영속화 로직이 없어 Marks & Numbers / Nature & Quantity of Goods 패널 수정해도 `house_bl_air_desc` 테이블이 갱신되지 않던 버그. `SeaHblUpdatePersistenceAdapter`/`TruckBlUpdatePersistenceAdapter`의 `applyDescSync` 패턴 동일 차용 — `HouseBlAirDescRepository` 의존성 주입, `airJpa` fetch 직후 descJpa fetch, `toAirDomain` 3번째 인자를 descJpa로 교체, `applyAirBlFields` 직후 `applyDescSync` 호출. private `applyDescSync`는 desc null no-op / 기존 row mutate / 없으면 신규 save. INSERT 경로(`saveExt`)는 Strategy가 이미 desc 처리 중이라 미변경. **SSOT 학습** — sub-set 매퍼 패턴(§6.37)은 setter 라인 자체 제거만으로 보호 의무가 부족 — form에서 편집 가능한 필드는 conditional setter 패턴이 필수. `<Mode>UpdatePersistenceAdapter` 신설 시 `applyDescSync` 동반 체크리스트 의무(§6.55 Detail 응답 `empty()` 정적 팩토리, §6.37 sub-set 매퍼와 함께 묶어 차기 도메인 진입 전 grep).

- **Air/Truck Customer PIC 제거 + Non B/L Cargo Package 80px + Truck Schedule/Performance 단열 배치 (2026-05-14 후속)** — 사용자 화면 검수 4건 일괄 정리. c1a6f7c AIR House Performance 패널 Customer PIC 항목 단순 삭제(schema의 `customerPic`은 Truck B/L 사용 중이라 schema 미변경). 922eb0a Truck B/L Entry Customer PIC 완전 제거 — `truck-performance-panel.tsx` 항목, `truck-bl-schema.ts` `customerPic` z 필드, `truck-bl-defaults.ts` 기본값 라인 3곳 일괄 정리. 79efde8 Non B/L Entry Cargo Package CodeBox 80px 폭 — SEA House `f992ffe` 패턴 차용(panel scope `nonbl-cargo-panel` + `widgets.css` 한 줄 + CodeBox wrapper `<div style={{ flex: "0 0 80px" }}>`). 3cbde7d Truck B/L Entry Schedule 4 widget 분리(`TruckScheduleVessel`/`TruckScheduleDates`/`TruckSchedulePol`/`TruckSchedulePod`) + Performance `cols={1}` — `FieldItemGrid`의 기본 `cols=2` + single item이면 split single로 좌측+빈슬롯 렌더되던 문제를 widget 분리 + `cols={1}` 명시로 해소(Vessel만 `cols=2` 유지하여 "Vessel | empty" 의도된 배치). useFieldLayout zustand persist는 사용자 로컬 layout `cols`와 코드 cols가 다르면 자동 재배치 트리거. **SSOT 학습** — `FieldItemGrid`에서 한 widget이 한 행 전체 폭을 점유하길 원하면 `cols={1}` 명시 의무(default 2 + isSingle은 좌측+빈슬롯). 화면 검수 의무 항목.

- **Truck B/L Entry 풀 마이그레이션 (2026-05-12)** — Non B/L Entry 패턴을 그대로 적용한 두 번째 풀 마이그레이션 사례. **(BE)** `TruckBlController`에 `POST /api/truck-bl`(create→`{id}`, §6.29)·`PUT /{id}`(update→`Void`, §6.35)·`DELETE /{id}`·`POST /find-by-hbl-no`(EXACT PK, §6.19)·`PUT /{id}/hbl-no`(Change BL No, §930) 5개 endpoint 신설. DTO 4종(`CreateTruckBlRequest`/`UpdateTruckBlRequest`/`ChangeTruckBlHblNoRequest`/`FindTruckBlByHblNoRequest`) §6.25 BE SSOT 적용(UI required `hblNo`/`bound`/`polCode`/`podCode`/`etd`/`eta`/`actualCustomerCode`/`operatorCode`/`teamCode`/`salesManCode` 10개 `@NotBlank` 유지, `UpdateTruckBlRequest`에 `hblNo` 자체 제외 §10 SSOT). `TruckBlPersistencePort` + `TruckBlUpdatePersistenceAdapter` 신설(§6.35 패턴). `TruckBlService`는 자기 jobDiv(TRUCK) 직접 알아 `houseBlPort.deleteByIdAndJobDiv(id, JobDiv.TRUCK)` 호출. `HouseBlFactory`에 `applyTruckCreate/applyTruckUpdate` 메서드 추가(305→335줄, 300줄 분리 검토 대상으로 `HouseBlTruckSubFactory` 향후 분리 권장). `Create/UpdateHouseBlCommand` record에 `TruckDetailCommand` nested record 추가 — 함정: `NonBlPersistenceAdapterTest.emptyCommand()` 파라미터 53→54 컴파일 오류 발생, null 추가로 수정(사용자 사후 보고). 신규 테스트 5종 29 케이스 전원 PASS, 전체 642 테스트 그린. **(FE)** `truck-bl-schema.ts`/`truck-bl-defaults.ts` 신설(Non B/L 패턴, `HouseBlFormValues` 의존 완전 제거). Toolbar 5→4 재구성(`Truck B/L No`/`Bound`/`Load Type`/`Service Term`, 기존 `Settle`은 Performance 패널의 `settlePartnerCode`로 입력 경로 유지, `Incoterms`/`Freight Term`/`Status`는 truck b/l 도메인 미사용으로 단순 제거). 작업 중 `truck-panels.tsx` 411줄로 증가 → 5개 패널 파일(party/schedule/cargo/document/performance)로 강제 분리, 기존 파일은 7줄 배럴로 축소. `truck-marks-panel.tsx`/`truck-description-panel.tsx` truck 전용 신설(House-BL 공유에서 분리). `pkgUnit`은 §10 Non B/L 정책 따라 `CodeBox kind="code-only"` 자유 텍스트(기존 `<select>` 7개 옵션 사라짐). `truck-order-grid-panel` cell SSOT(§6.12) `TextBox/NumberBox variant="cell"`로 교체. `use-truck-bl-entry-mutations.ts`(88줄) 신설(create/update mutation 분리 §6.29, 공용 confirm 모달 §959). `truck-bl-submit.ts`(114줄) `buildTruckBlCreateRequest`/`buildTruckBlUpdateRequest`(hblNo destructure 제외 §10 SSOT, 자식 row `id` 포함 §6.28). `truck-change-bl-no-modal.tsx` truck 전용 신설 — Non B/L `change-bl-no-modal.tsx`는 `nonBlPort`/`["non-bl",...]` queryKey 하드코딩이라 prop 분기 불가, 동일 구조로 truck 전용 신설. Entry 결선: hot-marker `truck-bl-entry:hot:${id}`(§6.16), F5 redirect, EXACT Search(§6.19), onSave 미조회 가드 toast.info, Enter implicit submit 차단(§6.24), detail useQuery `staleTime:Infinity`+`refetchOnMount:false`(§6.36). `truck-bl-grid.tsx` 더블클릭 hot-marker 추가 — §10 메모리상 "이미 적용"으로 표시됐으나 실제 미적용 상태였음, Phase C에서 보정. FE lint 0 error/build PASS.

- **Sea Master Entry post-migration 풀스택 정합 마무리 (2026-05-15 22:00 ~ 2026-05-16 01:47)** — 9 Phase 완료 후 사용자 화면 검수에서 누적 발견된 결함 13건을 BE 4 commit + FE 9 commit으로 일괄 해소. Air Master 진입 시 동일 패턴 직행 가능하도록 SSOT 정렬. **(1.1 House BL List 16 필드 BE 노출 보강, 2026-05-15 22:00, `85efd1f9`)** Master Entry House B/L List 그리드 14컬럼 중 6컬럼이 BE 미반영이던 결함 — `ConsoledHouseBlSummaryView`에 hblNo/shipperCode/consigneeCode/docPartnerCode/pkgQty/pkgUnit/weightUnit/grossWeightKg/cbm/etd/eta/vesselName/voyageNo/polCode/podCode 16 필드 노출 + `HouseBlPort.findConsoledSeaSummariesByMasterBlId` projection 신설 + `HouseBlRepositoryImpl` QueryDSL JOIN. FE 도메인 `ConsolidatedHouseBlSummary` 12→16 필드 확장, `master-house-bl-grid.tsx` `<colgroup>` 14컬럼 너비 강제. **(1.2 Container 패널 신설 — 콘솔 SEA 컨테이너 BE 응답 + FE 그리드 바인딩, 2026-05-15 22:27, `f32e70b9`)** SEA Master에 콘솔된 자식 HBL의 컨테이너를 row 단위로 모두 반환하는 신규 projection — `ConsoledSeaContainerView`(houseBlId/containerNo/containerType/sealNo1/2/3/pkgQty/pkgUnit/grossWeightKg/cbm/vgmKg 11 필드, 표시 전용, BigDecimal Jackson 직렬화 → number) + `HouseBlPort.findConsoledSeaContainersByMasterBlId` + `MasterBlDetailResult.consoledSeaContainers` 노출. FE `master-container-grid.tsx` 신설(매개변수 0개 함수, ReadOnly, +/- 없음, 10컬럼 colgroup), `main-sea.tsx` registry `container-master col=2 row=4 colSpan=4 rowSpan=2` 신규. **AIR Master는 컨테이너 미사용이므로 본 패턴 미적용** — Air Master는 dim 그리드 별도 패턴 필요. **(MasterBlFactory SRP 분리, 2026-05-15 22:54, `81f8ffdc`)** `MasterBlFactory` 383줄 → 265+118+60줄 3파일로 SRP 분리. `MasterBlSubFactory`(공통 sub-entity: dim/scheduleLeg/airCharge/desc), `MasterBlSeaSubFactory`(SEA 확장 필드: linerCode/vesselVoyage/serviceTerm/blType/onboardDate/rton/lineBkgNo/issueDate). **Air Master 진입 시 `MasterBlAirSubFactory` 신설 권장** (AIR 확장 필드 응집). **(detail 응답 zod schema 갭 통일, 2026-05-15 23:15, `7046d71a`)** §6.49 ⑧ 패턴 — FE adapter `master-bl.ts` zod schema에 `houseBlId`(BE Integer → number 통일, schema 키도 `houseBlId`로 SSOT), BigDecimal Jackson 기본 직렬화 = number 정합 모든 필드(grossWeightKg/cbm/vgmKg) `z.number().nullable().optional()`, `consoledSeaContainers`(11 필드 nested array) 신규 schema 추가. **(List 화면 스크롤 위치 기억 — useScrollRestore 공통 인프라, 2026-05-15 23:28, `2198e4ae`)** scroll event 저장 + RAF 폴링 복원 훅(`use-scroll-restore.ts`) + `GridList`에 `scrollPositionKey?: string` prop 추가 + 8 도메인 List 적용(`scrollPositionKey={\`list-scroll:<domain>:<variantKey>\`}`). 본 인프라는 모든 도메인 List 공통. **(List 그리드 셀 드래그 overlay 가상 스크롤 spacer tr 건너뛰기, 2026-05-15 23:44, `4cb7fbfc`)** virtual scroll의 spacer tr(높이 보정용 빈 행)을 selection overlay 좌표 계산이 row index에 포함시키던 결함 — `data-row-key="__virtual_spacer__"` 식별 후 overlay 계산 시 건너뛰기 가드 추가. **(Master BL Entry 메뉴 재진입 시 값 소실, 2026-05-16 00:03, `a060b3d4`)** sidebar 메뉴 재클릭 시 store는 유지하나 query cache miss로 detail 빈 폼 노출되던 race — `useEntryFocusStore`에서 `entryFocusKeys.masterBl(variantKey)` 헬퍼 + `useQuery enabled: !!id` 가드 정합. **(Master Entry Search EXACT endpoint 정렬, 2026-05-16 00:11, `1d1531a0`)** Entry Search가 List용 LIKE+COUNT 재사용하던 흐름을 폐기하고 `POST /api/master-bl/find-by-mbl-no` 신설 — `MasterBlUseCase.findMasterBlKeysByMblNoExact` + JOIN/projection/COUNT 제거, `select master_bl_id where job_div='SEA' and mbl_no=? order by created_at desc, id desc limit 2`만 발사. FE `masterBlPort.findByMblNo` 어댑터 추가, Entry `handleSearch` dummy filter 객체 제거. **(MasterBlDetail 응답 root 4 필드 노출 + DescProjection 통합, 2026-05-16 01:30, `20293538`)** §6.49 ⑬ + ⑱ 풀스택 결함 — 도메인엔 보유(`MasterBl.java:59~60` mainItemName/hsCode, :61 settlePartnerCode CustomerCode VO, :140 MasterBlDesc desc)하나 `MasterBlDetailResult`/`MasterBlDetailResponse`/`MasterBlFactory.toDetailResult` 모두 미노출. 4 필드 root 노출 + `SeaDescProjection` → **`DescProjection`** rename(SEA/AIR 공통 사용) + `SeaDetailProjection`/`SeaDetailResponse`에서 `desc` 필드 제거(root로 승격). **AIR Master Entry 진입 시 동일 패턴 — `airDetail.desc` 별도 두지 말 것, root `desc`로 통일**. 기존 테스트 2건 수정(사용자 승인) + 신규 케이스 1건. **(Master Entry Marks/Desc/Remark/Main Item/HS Code/pkgUnit/Settle Partner 표시 정합, 2026-05-16 01:39, `5f3b3f0d`)** §6.49 ⑯ form key 4축 정합 — FE Panel은 root `desc.marks/description/descClause1/descClause2/remark`를 register 중이었으나 mapping은 `seaDetail.desc.*`만 채움 → 화면 빈값. SSOT 결정: FE 중복 필드 `cargoMainItem`/`cargoHsCode`/`settlePartner` 제거 → BE 도메인 이름 `mainItemName`/`hsCode`/`settlePartnerCode` SSOT 통일. `map-master-bl-detail.ts`에 pkgUnit + root desc + 4 필드 매핑 추가, `master-cargo-doc-panel.tsx` register name 통일, `master-bl-submit.ts` `buildSeaDetailRequest`에서 `sd.desc.*` 참조 제거(root desc로 일원화), zod adapter schema 동기. **AIR Master Entry 진입 시 동일 SSOT — schema/defaults에 `cargoMainItem`/`cargoHsCode`/`settlePartner` 만들지 말 것, BE 도메인 이름 그대로 사용**. **(consolidated-hbl-list badge 영역 제거, 2026-05-16 01:43, `c80f40db`)** `master-bl-entry.tsx`에 있던 별도 badge 영역(`detail.consolidatedHouseBls.map((hbl) => <span className="badge">{hbl.hblNo}</span>)`)이 House B/L List 패널(`master-house-bl-grid.tsx`)과 중복 정보로 fallback이었음. CSS 클래스 정의 부재로 스타일 영향 없음, `ConsolidatedHouseBlSummary` import도 함께 제거. **AIR Master Entry에도 동일 패턴 — 별도 badge 영역 만들지 말 것**. **(Master Entry House B/L List hbl_no 더블클릭 → House Entry 진입, 2026-05-16 01:47, `5a8cbbb6`)** B/L List 더블클릭 패턴(invalidate → clearDraft → setFocus → hot-marker sessionStorage → addTab → router.push)을 Master Entry 내 House B/L List 패널에도 적용. master variant.key(sea-exp/sea-imp/air-exp/air-imp)를 그대로 house variant.key로 매핑(비즈니스 룰: SEA Master에 SEA HBL만, AIR Master에 AIR HBL만 콘솔). `master-house-bl-grid.tsx`는 SEA/AIR 공통 컴포넌트라 **AIR Master에서도 별도 변경 없이 자동 동작 — Air Master Phase에서 본 패턴 별도 작업 0건**. **SSOT 학습 — 본 라운드 신설 매트릭스 항목**: phase1-setup.md §6.49 매트릭스 ㉕~㉘ 추가(Master root `desc` SSOT / consolidated-hbl-list badge 영역 금지 / Master House B/L List hbl_no 더블클릭 패턴 / MasterBlFactory SRP 분리 권장 패턴), plan 체크리스트 24~26 추가. **Air Master Entry 진입 전 §13.9 신설** — §13.7·§13.8 체크리스트 재사용 + 본 라운드 추가 점검 항목 명시.

- **§6.62 Master B/L Entry 무수정 저장 시 address UPDATE 회귀 fix (2026-05-16)** — List 더블클릭 → Entry → 무수정 저장 시 `master_bl`의 shipper/consignee/notify address 3컬럼이 DB NULL인데도 `update set ...=NULL`이 발사되던 회귀. p6spy 임시 `@DynamicUpdate` 진단으로 dirty 컬럼을 좁혀 두 곳의 동시 원인 확정. **(BE)** `MasterBlFactory.applyToEntity`/`toEntity`의 `Nullables.mapOrElse(cmd.shipperCode(), CustomerCode::of, ...)` 패턴에서 `CustomerCode::of` method reference가 `Function<T,R>` signature 때문에 **1-arg `of(value)`로 resolve** → `cmd.shipperAddress`가 form에서 정상 도달해도 항상 무시되어 도메인 address가 null로 덮어쓰이던 버그. 2-arg `CustomerCode.of(value, address)` explicit call + `shipperTouched`/`consigneeTouched`/`notifyTouched` 분기로 PATCH 의미론 유지. **(FE)** `master-bl-entry.tsx` useEffect의 `detailLoadedRef.current = true`가 draft 가드 **이후**에 set되던 순서 — cache stale 상태에서 draft↔detail race로 잔류 form values가 form에 들어옴. House 패턴(`house-bl-entry.tsx:154-161`) 정합으로 `detail 도착 즉시 ref true → draft 가드` 순서로 변경. 새로고침 후 정상 vs 새로고침 전 비정상 패턴이 결정적 단서였음. **AIR Master / 차기 Entry 진입 시 §6.62 의무 grep**: (1) Factory 복합 VO method reference 1-arg resolve 안티패턴 확인 (2) Entry useEffect ref 잠금 순서 House 패턴 정합 확인 (3) 무수정 조회→저장 p6spy 0건 회귀 테스트.

- **§6.65 SEA Master Entry FE-BE 검증 정합 + Master B/L Create 흐름 SELECT 4→0 + Port saveMasterBl 분리 + toolbar nested path TextBox cleanup 회귀 fix (2026-05-16)** — 사용자 화면 검수 라운드 3 commit으로 일괄 정리. **(0bb51d18 — FE-BE 검증 정합)** SEA Master Entry INSERT 시 "freightTerm은 null이어서는 안 됨" 토스트가 노출되던 회귀. FE `master-bl-entry.tsx` is-required 화이트리스트는 `["MBL No", "MAWB No", "Master Ref"]` 3개뿐인데 BE `CreateMasterBlRequest`가 `@NotNull String freightTerm` + `@NotNull @NotBlank(SeaMasterGroup) String shipmentType` 으로 임의 필수화. 두 어노테이션 제거(`String freightTerm` / `String shipmentType`). 나머지 SEA is-required 필드(mblNo/masterRefNo/pol/pod/etd/eta/operator/team + seaDetail.linerCode/vesselName/voyageNo)는 `@NotBlank(SeaMasterGroup)` 그대로 유지. UpdateMasterBlRequest는 이미 모든 필드 nullable이라 무변경. **SSOT** — 향후 도메인 진입 시 FE entry 화면의 is-required 화이트리스트와 BE Create Request의 `@NotNull/@NotBlank` 매핑 정합 의무 grep. **(b3f3c112 — Port saveMasterBl 분리 + Create 흐름 SELECT 4건 제거 + desc 헬퍼 SRP 분리)** p6spy 로그에서 SEA Master Create 시 불필요 SELECT 4건 발사: pre-insert 2건(`master_bl_sea`/`master_bl_sea_desc` findBy* 무조건 호출) + post-insert 2건(`saveMasterBl` 마지막 `loadWithExt(savedJpa)`가 derived query로 sub/desc 재조회 — JPA 1차 캐시는 PK 기반 `findById`에서만 hit, derived query는 매번 SQL 발사 §6.34). 호출자 분석: `MasterBlService.createMasterBl`은 `saveMasterBl` 반환에서 `getId()`만 추출(Controller `Map.of("id", id)`), `updateMasterBl`(AIR)은 반환값 버리고 직후 `findEntityById(id)` 재조회 → 두 흐름 모두 `loadWithExt` 결과 미사용 확인. **Port 분리**: `MasterBl saveMasterBl(MasterBl)` 제거 → `Long createMasterBl(MasterBl)`(신규 영속화 후 ID 반환) + `void updateMasterBl(MasterBl)`(기존 row update, 호출자가 필요 시 재조회). **Adapter**: `createMasterBl`는 parent · sub insert + `createXxxDescIfPresent` 호출 + `savedJpa.getMasterBlId()` 반환, `loadWithExt` 호출 안 함. `updateMasterBl`는 `findById(id).orElseThrow(ResourceNotFoundException)` → applyCommonFields → save → sub `findByMasterBlMasterBlId.orElseGet(new)` → save → `updateOrDeleteXxxDesc` → 반환 없음. **desc 헬퍼 SRP**: 기존 `saveOrDeleteSeaDesc/AirDesc`는 create insert · update overwrite · delete · isNew 분기까지 4 책임 혼재 → `createSeaDescIfPresent`(domainDesc null no-op, else `new ...()` + save, SELECT 없음) / `updateOrDeleteSeaDesc`(domainDesc null → findBySea ifPresent delete / else findBySea orElseGet new + save) 분리. AIR 동일(`createAirDescIfPresent` / `updateOrDeleteAirDesc`). `isNew` 플래그 파라미터 사라짐 — 각 헬퍼가 자기 책임을 자체적으로 알게 됨. **Service**: `port.saveMasterBl(domain).getId()` → `port.createMasterBl(domain)`, AIR Update는 `port.updateMasterBl(domain)` (반환 무시 그대로). 직후 `findEntityById(id)` 재조회 유지. **테스트**: `MasterBlPersistenceAdapterTest` 3건 시그니처 갱신(`createMasterBl_sea_savesSeaExtAndSkipsDescPreSelect` / `createMasterBl_air_callsSyncMethodsInOrderAndSkipsDescPreSelect` / `updateMasterBl_whenNotFound_throwsResourceNotFound`) + `loadWithExt` 제거로 미사용된 `mapper.toSeaDomain/toAirDomain` stub 정리(사용자 옵션 1 승인). **결과** — SEA Create + desc 있음 기준 6 SQL → 4 SQL(pre-SELECT 1 `existsByMblNo` + INSERT 3). **AIR Master Entry 진입 시 동일 패턴 적용 의무**: `loadWithExt` 호출 제거 + `MasterBlAirSubFactory` 신설 시 `createAirDescIfPresent`/`updateOrDeleteAirDesc` 패턴 차용. `loadWithExt` 메서드 자체는 `findMasterBlById`/`searchMasterBls` 등에서 계속 사용되므로 보존. **(7cd0d950 — toolbar nested path TextBox cleanup 회귀 fix)** 해상 마스터 삭제 후 toolbar의 'Line Bkg. No' 필드만 stale 값으로 남던 회귀. `master-bl-entry.tsx` toolbar render에서 TextBox 분기는 `form.register`(uncontrolled) 패턴, ComboBox는 이미 Controller 패턴이었음. root path TextBox(MBL No → `mblNo`, Master Ref → `masterRefNo`)는 cleanup 정상 — 유일하게 nested path(`seaDetail.lineBkgNo`) + register 조합인 'Line Bkg. No'만 `form.reset(defaults)` 호출 시 `setValue('seaDetail.lineBkgNo', undefined)`가 `input.ref.current.value`를 안정적으로 갱신하지 못해 화면에 직전 입력값 잔류. **fix** — toolbar TextBox 분기를 `Controller`(controlled component)로 통일. `value={(field.value as string | undefined) ?? ""}` fallback로 undefined → "" 보장. 결과: toolbar 모든 입력이 일관된 Controller 패턴이 되어 root/nested path 구분 없이 reset 시 안정 cleanup. `master-bl-defaults.ts` / `master-bl-schema.ts` / `map-master-bl-detail.ts` 무변경(Controller fallback로 처리). **SSOT 학습** — RHF nested path + uncontrolled register 안티패턴. toolbar/패널 어디든 nested path 입력은 Controller 권장. **AIR Master Entry 진입 시 동일 grep 의무**: toolbar TextBox 분기가 nested path를 register로 처리하는지 확인. 형제 도메인(House BL / Truck BL) toolbar도 동일 점검 후속 작업 예정.

- **§6.63 SEA Master Update 흐름 SELECT 쿼리 절감 (2026-05-16, 2단계)** — Entry 수정 후 저장 시 PUT 1회당 SELECT 9회 + GET refetch 5회 = 총 14회 발사. 사용자 예상치 10회 대비 4회 초과로 비효율 진단. **1단계** (PUT 9→5): **(원인 1)** `MasterBlService.updateMasterBl`의 첫 `findEntityById(id)`가 jobDiv 분기(SEA/AIR) 결정용인데 entity 전체 fetch → `master_bl_sea` + `master_bl_sea_desc` 2개 query 동반 발사. **(원인 2)** `SeaMasterPersistencePort.update`가 void 반환이라 응답 빌드 시 마지막 `findEntityById(id)`로 재구성 → `master_bl_sea` + `master_bl_sea_desc` 2개 query 다시 발사(1차 캐시는 PK 기반만 작동, query method는 매번 SQL — §6.34). **fix 1**: 첫 findEntityById → `masterBlPort.findJobDivById(id)`로 jobDiv만 조회(1 query), AIR 분기에서만 full fetch. **fix 2**: `SeaMasterPersistencePort.update` 시그니처 `void → MasterBl`, `SeaMasterUpdatePersistenceAdapter.update`가 끝에 첫 `toSeaDomain` 호출의 domain 객체 그대로 반환(applyToEntity로 cmd 반영 + applyDescSync로 desc 동기화 후 상태라 reload 불필요, `toSeaDomain` 재호출 금지 — 테스트 verify times(1) 정합). **2단계** (PUT 5→4): `findJobDivById` 자체도 불필요 — FE 가 variant(`sea-exp`/`air-imp` 등)로 jobDiv 결정 후 PUT body 의 `cmd.jobDiv()` 에 항상 송신하므로 BE 에서 DB lookup 없이 `MasterBlJobDiv.valueOf(command.jobDiv())` 로 신뢰 가능. 변조 시 fail-safe 두 곳: **SEA 분기**는 `SeaMasterUpdatePersistenceAdapter` 내부 `parentJpa.getJobDiv() != SEA` 검증, **AIR 분기**는 `saveMasterBl` 의 `switch (domain) default -> throw IllegalArgumentException` (도메인 타입은 DB 기반이라 cmd 변조 무영향). 결과: PUT SELECT 4회, 총 9회로 정렬. **AIR Master는 기존 흐름 유지** — Air Master 마이그레이션 시 동일 패턴 적용 의무(§13.9 잔여): saveMasterBl 결과 사용 + 마지막 findEntityById 제거.

- **§6.67 B/L Entry Search·Change B/L No 모달 resolve 후 draft 가드 해제 fix (2026-05-16)** — House/Master B/L Entry(SEA/AIR)에서 (A) toolbar Search로 동일 B/L 재조회 분기 + (B) Change B/L No 모달 onSuccess 모두 `queryClient.invalidateQueries(["xxx-bl","detail",id])`로 detail GET을 정상 재발동했음에도, 사용자가 draft 작성 중이거나 직전 draft가 store에 남은 상태면 `useBlDraftSync`의 `didRestoreFromDraftRef.current === true`로 잠겨 detail useEffect의 `if (didRestoreFromDraftRef.current) return;` 가드(`master-bl-entry.tsx:112` · `house-bl-entry.tsx:158`)에서 `form.reset(mapXxxDetailToForm(detail))`이 skip → BE 응답은 갱신되지만 화면이 옛 값/draft 값 그대로 잔류. 사용자 체감으로는 "캐시 무효화가 안 됨". 특히 Master는 응답에 `consolidatedHouseBls` 그리드가 묶여 있어 콘솔 House가 다른 화면에서 수정된 뒤 동일 mbl_no Search 시 form.reset 누락이 곧 콘솔 House 그리드 stale로도 직결. **fix(FE)**: (A) `house-bl-entry.tsx`의 `useSearchBl(form, variant, { id, onAfterFound })` 콜백에 `didRestoreFromDraftRef.current = false` 1줄 추가(기존 `detailLoadedRef.current = false` 위임 패턴 동일 위치). Master는 `master-bl-entry.tsx:handleSearchBl`의 `if (targetId === id)` 분기에서 `clearDraft` 다음 줄에 동일 1줄 추가 — `useQuery` 옵션(`staleTime: Infinity` / `refetchOnMount: false` / `gcTime: Infinity` / `structuralSharing: false`)은 변경 금지, 메뉴 이동 후 복귀 시 캐시 보존(GET 미발생 + draft 유지) 기존 동작 유지. (B) `<HouseChangeBlNoModal>` / `<MasterChangeBlNoModal>` 호출처 onChanged 콜백을 한 줄에서 다중 줄로 풀어 `didRestoreFromDraftRef.current = false` + `clearDraft(\`house:${variant.key}:${id}\`)` / `clearDraft(\`master:${variantKey}:${id}\`)` 2줄 추가 — 모달 컴포넌트 자체(`master-change-bl-no-modal.tsx` / `house-change-bl-no-modal.tsx`)는 무변경, 책임을 호출처에 위임하는 현 패턴 유지. **SSOT 학습 — invalidateQueries 책임 위임 규약**: `useBlDraftSync`의 `didRestoreFromDraftRef`는 **key 변경 시에만** 자동 재계산(useEffect deps). 동일 key 상태에서 detail invalidate만으로는 form.reset이 보장되지 않으며, invalidate 책임을 가진 호출처(toolbar Search · 외부 모달 onSuccess 등)는 ① `detailLoadedRef.current = false` ② `didRestoreFromDraftRef.current = false` ③ `clearDraft(<해당 key>)` 세 가지를 묶어 호출할 의무. **AIR Master / House Air / Truck B/L 등 형제 도메인 진입 시 동일 grep 의무**: Entry 컴포넌트의 외부 mutation 콜백(`onAfterFound` / `onChanged` / `onSuccess` 직접 핸들러)에서 위 3종 reset 누락 여부 점검. **수정 파일**: `front-end/src/components/fms/house-bl/house-bl-entry.tsx:223,400-401` · `front-end/src/components/fms/master-bl/master-bl-entry.tsx:146,316-318`. lint·build PASS, BE 무변경.

- **§6.69 AIR Master Entry 패널 배치 House 정합 — FE 패널 12 신설 + Performance 제거 (2026-05-16, Phase A1~A3)** — §6.68 직후 추가 라운드. 사용자 요청: "AIR House Entry와 AIR Master Entry는 House AWB List 아래 배치가 동일해야 함, 단 Party 패널 Doc Partner는 제외." 초기 plan은 BE Master 도메인을 House에 정합하도록 7필드(actualCustomerCode/salesManCode/incoterms/salesClass + airDetail.fhd/cargoType/originOfGoods) + 3메서드(assignOperator 4-arg / updateTradeInfo 4-arg 오버로드 / assignSalesInfo) 확장하는 방향이었으나 작업 중 사용자 재결정: **Master에는 위 7필드가 모두 불필요** — Document 패널은 Operator/Team/Settle Partner 3슬롯, Performance 패널 제거(Settle Partner를 Document로 흡수), Trade 패널은 incoterms·fhd 제외, 나머지는 House 그대로. **(Phase A1 — BE 도메인 추가분 롤백)** Phase A1에서 추가한 `MasterBl` 4필드+3메서드 / `MasterBlAir.AirFields` 3필드 / `MasterBlAirSubFactory`·`MasterBlMapper`·`MasterBlMapperTest` 시그니처 propagate 모두 `git checkout HEAD --` 으로 commit `8c3b247b` 상태 복원(5 파일, working tree clean). build PASS, 776 테스트 통과. **(Phase A2 — FE 패널 교체, commit 16307b26)** **신설 11 + 재작성 1 + main-air registry 분리**: `master-air-{party,schedule,document,trade,issue,cargo,handling-info,dimension,charge-info,marks,nature-goods,remark}-panel.tsx`(12 파일). 격리 원칙 — House 패널 직접 import 0건, 모든 신설 패널이 `useFormContext<MasterBlFormValues>()` 직접 사용. `main-air.tsx` (Master)을 House처럼 EXP/IMP 분리(`MASTER_BL_AIR_EXP_REGISTRY` 13 위젯 / `MASTER_BL_AIR_IMP_REGISTRY` 12 위젯, IMP는 Issue 제외). 좌표 정합: **House `HOUSE_BL_AIR_EXP_REGISTRY` / `HOUSE_BL_AIR_IMP_REGISTRY` 좌표를 그대로 복사 + row +2 오프셋** + 최상단 `master-house-bl-grid`(col 0 row 0 colSpan 6 rowSpan 2). House Performance 위젯 자리(col 4 row 4 EXP rowSpan 1 / IMP rowSpan 2)는 **빈 공간으로 유지**(다른 패널 확장 금지, 시각 검증 후 사용자 결정). `main-tab.tsx` form prop 제거. **차이 처리**: (1) Party 패널 House는 4슬롯(S/C/N/DOC) → Master는 3슬롯(S/C/N), CONSIGNEE IMP required. (2) Document 패널 House는 4슬롯(Sales Man/Operator/Team/Sales Class) → Master는 3슬롯(**Operator/Team/Settle Partner**) — Performance 패널 흡수, BE에 없는 Sales Man/Sales Class 제외. (3) Trade 패널 House는 9필드(currency/incoterms/freight-term/other-term/dv-carriage/insurance/dv-customs/account-info + fhd IMP) → Master는 7필드(**incoterms·fhd 제외**), 모두 `airDetail.*` nested + freightTerm. (4) Nature & Quantity of Goods는 `desc.description` 단일(House 그대로). (5) Handling Info는 `airDetail.handlingInformationCode/Text` (Master는 `Text`, House는 `Desc` — Master 필드명 사용 정합). (6) Schedule은 `polCode/podCode` (Master, House는 `pol/pod`) 정합. (7) Dimension `dims` 그리드 6컬럼, House는 `string` / Master는 `number | undefined` — `EMPTY_DIM_ROW`를 `undefined`로 초기화. **해제 (Master AIR registry에서 제거, SEA 사용은 유지)**: `MasterPartyPanel`/`MasterSchedulePanel`/`MasterCargoDocPanel`/`MasterContainerDimPanel`/`MasterMarksPanel`/`MasterGoodsDescPanel`/`MasterRemarkPanel`/`MasterAirChargesPanel` — 모두 `main-sea.tsx`에서 계속 사용되므로 코드 보존, 등록만 제거. **BE 변경 0건 / Master schema/defaults/submit/mapper 변경 0건 / SEA 영향 0**. lint·build PASS. **SSOT 학습 — BE 도메인 영향 0으로 House 정합 패턴 확립**: 다른 도메인(Truck B/L 등)에서 동일 패턴 적용 시 (1) 시각 정합을 위한 패널 신설은 격리 원칙으로 `<domain>-air-*-panel.tsx` 신설(House 패널 직접 import 금지), (2) Master/House 도메인이 서로 다른 BE 필드를 가지면 사용자 결정으로 BE 확장 vs FE-only 표시 vs 필드 제외 분기. 본 라운드는 "필드 제외 + 일부 패널 흡수" 패턴. **잔여 후속**: Performance 자리(col 4 row 4) 빈 공간 정리(다른 패널 확장 또는 그대로 유지)는 사용자 시각 검증 후 결정. AIR Master Entry 화면 사용자 검증 OK 후 push 또는 회귀 fix.

- **§6.68 AIR Master Entry/Detail 마이그레이션 풀스택 완주 (2026-05-16, Phase 0~4+4-bis)** — `docs/migration/phase1-setup.md §13.9` 차기 타깃 의무 점검 SSOT를 처음부터 끝까지 적용해 AIR Master Entry/Detail BE+FE 풀스택을 SEA Master 학습(§6.62/§6.63/§6.65/§6.67)과 완전 정합으로 완성. **(Phase 0, b13f87d0)** `master-bl-entry.tsx` 324→222줄 분리 — `use-master-bl-entry-handlers.ts`(133, handleSearchBl/Reset/ChangeBlNo/Save/Delete) + `master-bl-entry-page-head.tsx`(65) + `use-master-bl-entry-detail-sync.ts`(52, useQuery+draft useEffect) 3 파일 추출. AIR 분기 코드 가산 전 사전 부채 해소(§13.9-2). 외부 props 변경 0건. **(Phase 1)** §13.9-1 의무 grep(§6.48 7항목 + §6.49 ㉕~㉘ + §13.7~§13.9) 위반 0건. MasterBl/MasterBlAir 도메인 + MasterBlAirJpaEntity + 1:N 자식(MasterBlDim/MasterBlScheduleLeg/MasterBlAirCharge) + 1:1 MasterBlAirDesc 모두 기존 정의 완료 확인. AirMasterPersistenceAdapter는 Search만 — Create/Update 신설 필요. **(Phase 2, 32dc6649)** BE Entry/Detail UseCase·Adapter·검증 그룹. **신설 8**: `AirMasterGroup`/`AirImpMasterGroup`(검증 marker), `MasterBlAirSubFactory`(85, MasterBlSeaSubFactory mirror, applyAirCreate/applyAirUpdate, 1:N dims/scheduleLegs/airCharges 동기화), `AirDetailProjection`(33, `empty()` 정적 팩토리 §6.55), `AirMasterPersistencePort`(15), `MasterBlAirSubMapper`(162, applyMasterAirCommonFields/applyMasterAirFields/applyAirDescFields, null 가드 §6.37 + trim-aware compare §6.63), `AirMasterUpdatePersistenceAdapter`(100, SeaMasterUpdatePersistenceAdapter mirror, dirty-checking + applyDescSync SRP §6.62, domain 반환·재fetch 금지 §6.63·§6.65), `AirMasterBlControllerWebMvcTest`(296, 10 케이스: happy 5 + 400 검증 4 + 404 1, **@MockitoBean Validator 0건** §6.61). **수정**: CreateMasterBlCommand/UpdateMasterBlCommand에 `AirDetailCommand` sub-record + `airDetail` 필드, MasterBlFactory에 AirSubFactory 주입·호출(326줄로 증가, 보고 후 Phase 2C 보류), MasterBlService AIR update를 `airMasterPersistencePort.update(id, command)`로 교체(`findEntityById` 이중 호출 제거), CreateMasterBlRequest/UpdateMasterBlRequest에 `AirDetailRequest` sub-record + 공통 필수 9개에 `{SeaMasterGroup, AirMasterGroup}` 그룹 annotation + AIR EXP 전용 4개(`airlineCode`/`issueDate`/`issuePlace`/`signature`)에 `@NotBlank(AirMasterGroup)`, MasterBlDetailResponse에 `AirDetailResponse` inner record(`empty()` + `from()`, **`airDetail.desc` 신설 0건** §6.49 ㉕), MasterBlController createMasterBl/updateMasterBl에 AIR 분기(`AirMasterGroup`/`AirImpMasterGroup` 동적 검증, SEA 패턴 mirror), MasterBlAssembler에 `toAirDetailCommand`/`toAirDetailCommandU`. **기존 테스트 3 fix(사용자 승인)**: MasterBlAssemblerTest 4 케이스 `airDetail`/`airCharges` null propagate(32→33→34), MasterBlSeaDetailProjectionTest 생성자에 MasterBlAirSubFactory 인자 추가(2→3-arg), SeaMasterUpdatePersistenceAdapterTest는 시그니처 그대로 일치. MasterBlControllerWebMvcTest happy path 케이스의 잘못된 `jobDiv=AIR` 변경을 SEA EXP로 복원(AIR happy는 AirMasterBlControllerWebMvcTest로 분리 커버). 결과: build PASS, 776 테스트 통과. **(Phase 3, 4530747a)** FE-UI 카탈로그. **신설 1**: `master-air-issue-panel.tsx`(115, house-bl `air-issue-panel.tsx` mirror, issueDate/issuePlace/signature, Air 모드에서만 표시). **수정 5**: master-bl-schema에 `AIR_DETAIL_FORM_SCHEMA`(18 필드: ) + `MASTER_BL_SCHEMA.airDetail` nested, master-bl-defaults airDetail 18 빈값, main-air.tsx registry에 `air-issue` 위젯 등록(col 0 row 12 colSpan 3), master-air-charges-panel native input 8 컬럼 → `TextBox/NumberBox variant="cell"`(text 5 + numeric 3), master-container-dim-panel AirDimsGrid 6 컬럼 → `NumberBox variant="cell" decimalPlaces=3`. **합계 14 native input → 카탈로그 교체**. **root 산재 AIR 필드 변경 0건** (rTon/volWeight/chargeWeight/coLoadAgent/coLoadType 등 legacy 호환 유지, 신규 SSOT는 `airDetail.*`). HTML5 required/zodResolver 0건(BE SSOT, 메모리 `feedback_validation_be_ssot`). **(Phase 4+4-bis, 1e95f1b3)** FE-Data 정합 + 검증 누락 fix. domain/master-bl에 `AirDetailRequest`/`AirDetailResponse`/`Dim`/`ScheduleLeg`/`AirCharge` 타입 5종 신설(312줄, 300줄 초과 보고 — 타입 정의 ARCH2 예외 후보). adapter/out/api/master-bl에 `AIR_DETAIL_SCHEMA`(18)/`DIM_API_SCHEMA`(6)/`SCHEDULE_LEG_API_SCHEMA`(7)/`AIR_CHARGE_API_SCHEMA`(8) + `MASTER_BL_DETAIL_SCHEMA` extend +4, nullable transform `v ?? undefined` 통일(§6.49 ⑮). map-master-bl-detail에 airDetail/dims/scheduleLegs/airCharges 매핑. master-bl-submit에 `buildAirDetailRequest` + AIR 분기. master-change-bl-no-modal title `Change {blNoLabel}`로 mode-aware. **§6.65 FE-BE 검증 정합 grep** 후 누락 3건 fix: (1) `master-schedule-panel.tsx:51` AIR Airline 필드의 `seaDetail.linerCode` 잘못된 path → `airDetail.airlineCode`(추가 발견 보너스: issueItems의 `seaDetail.issueDate`/`signature`/`issuePlace`도 `airDetail.*`로 정합), (2) AIR 분기에 `polCode`/`podCode`/`etd`/`eta` SEA atom(`PolField`/`PodField`/`EtdField`/`EtaField`) 재사용으로 노출(BE @NotBlank 공통 필수인데 입력 UI 미노출 회귀), (3) `master-panels.tsx` party 패널에 `consigneeCode` IMP variant 분기 is-required 표시(`role==='CONSIGNEE' && isImp`). draft 가드 3단계(§6.67) `use-master-bl-entry-handlers.ts:71-73` + `master-bl-entry.tsx:214-216` 위치에서 보존 확인. mutation List 자동 invalidate 0건 확인(메모리 `feedback_list_entry_invalidate`). **누적 결과** — BE 신설 8 + 수정 9(MasterBlFactory 326줄 분리는 Phase 2C 보류) + 테스트 신설 1·수정 3. FE 신설 4(Phase 0 분리 3 + Phase 3 air-issue 1) + 수정 11. lint/build PASS, BE 776 테스트 통과. **§13.9 모든 점검 항목 누락 0건**: §6.55 nested null → empty() ✓, §6.63·§6.65 Port 반환값(Create=ID/Update=domain) ✓, §6.49 ㉕ desc root SSOT(`airDetail.desc` 0건) ✓, §6.61 Validator mock 0건 ✓, §6.62 desc SRP 분리(createAirDescIfPresent/updateOrDeleteAirDesc) ✓, §13.9 컨테이너 필드 0건 ✓, master-bl-entry.tsx 332→222줄(분리 완) ✓, MasterBlAirSubFactory 신설 ✓, AirDetailResponse 신설 ✓. **잔여 후속 항목**: (1) Phase 2C `MasterBlFactory` 326줄 분리(MasterBlAirSubFactory로 toAirDetailProjection 이전, 사용자 결정으로 별도 작업), (2) `master-air-issue-panel`(Phase 3 신설) vs `master-schedule-panel.tsx`의 `issueItems` 영역 중복 가능성 — 디자인상 두 패널에 Issue 필드가 분산되어 정리 검토. **AIR Master 진입 시 SEA 학습 반복 방지 체크리스트 10개를 처음부터 적용한 첫 도메인** — 차기 도메인(House Air/Truck B/L 등) 진입 시 동일 SSOT 적용 가능.
